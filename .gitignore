node_modules
.next
.data
/test-results
/playwright-report
*.tsbuildinfo
.env*.local

# Vorher-Screenshots fuer UX-Redesign (lokal)
.ux-vorher/

# Betriebsnotizen, lokal (Gruppenlisten, abgelesene Serverwerte) — nie committen
.betrieb-lokal/

# Arbeitsverzeichnis der Subagenten-Ausfuehrung (Ledger, Briefs, Reviewpakete).
# Reiner Kladdenraum je Plan — der Nachweis liegt in der Git-Historie.
.superpowers/

# IDE-Zustand, lokal. Konvention aus ../lagerbuch/.gitignore; in keinem der
# Nachbarprojekte ist .idea/ eingecheckt.
.idea/
.vscode/
