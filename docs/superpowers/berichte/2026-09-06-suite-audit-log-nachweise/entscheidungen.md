# Entscheidungen während der Umsetzung

Ruling: worktree under /private/tmp — writable sandbox isolation without altering main gitignore — cost: location is temporary until delivery.

Ruling: skill scripts are not executable; run workspace helper through bash and extract briefs directly — no change to plugin files — cost: equivalent helper output maintained locally.

Ruling: universal object-reference hashing with raw-reference search server-side — prevents bearer IDs and changing ID semantics leaking — cost: references need lookup instead of directly readable object IDs.

Ruling: code-based shared credentials use actor kind access, not user — avoids implying a confirmed human identity — cost: one additional displayed actor category.

Ruling: maintained docs/design remains the canonical UI authority instead of introducing DESIGN.md or a competing map — existing project instructions and approved UI context govern — cost: premium format scanner remains exit2 with documented map/filename/AntdSelect classification findings; actual UI and project gates assessed separately.

Final review2d6332f3 needs fixes F1objectnamespace,F2sevenreachablepagedenials,F3truthfuldeliveryresults,F4retryfixture; M1fieldvalidation/M2canonicaltable promotedintooneconsolidatedpolishwave. ONE finalfixwave active freshworker audit_final_fix, base2d6332f3. Ruling: exact module+objectType+hash scope for object detail filtering — minimal truthful identity boundary without rewriting retained references — cost: table changes and differently typed delivery events are separate filter scopes until an evidenced mapping exists. Fullbrowserinterrupted log will be retained aspre-fix; finalgates oncorrectedHEADrequired.

Ruling: preserve current darfPlanSehen always-true semantics, correct F2 reachability claim to six real denied pages plus one defensive currently unreachable plan branch — real DefaultPage probe establishes allowed foreign plan — cost: no new plan privacy restriction is introduced; defensive branch test is synthetic and disclosed. Fix instruments existingbranch without changing permission, adds real allowedprobe. Reviewer informed.
