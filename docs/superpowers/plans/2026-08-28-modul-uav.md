# Modul `uav` — Implementation Plan (Drohnen-Trainingsbegleiter, ganzes Modul + Import + Cutover-Vorbereitung)

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Die Alt-Anwendung `uav-praxis` (`../uav-praxis` @ `cd973b0`) wird als Suite-Modul `uav` nachgebaut, so dass nach dem Domain-Schwenk von `uav-training.iuk-ue.de` **alle vier verteilten Zugänge** — Dauer-Code, Magic-Link `/login?code=`, Session-Cookie `sid`, Offline-Zustand samt ungesyncter Queue — unverändert funktionieren.

**Architecture:** Neues Modul unter `src/app/m/uav/` mit eigener SQLite `uav.db` (Schema 1:1 aus `uav-praxis/server/db/schema.sql`, Spaltennamen und ISO-Text-Zeitstempel bleiben, damit der Import Zeile für Zeile kopieren und der Paritätscheck vergleichen kann). Die **Teilnehmer-Oberfläche** ist eine Client-Insel (portiert aus `uav-praxis/src/`), die wie bisher gegen `localStorage` und eine HTTP-API (`/m/uav/api/*`, unter dem Host-Rewrite extern `/api/*`) arbeitet — das ist die Voraussetzung dafür, dass Offline-Queue und Storage-Keys überleben. Die **Verwaltung** ist RSC + Server Actions in antd. Der Teilnehmer-Login ist eine modul-eigene Session (Cookie `sid`, SHA-256-Hash in `sessions`), der Admin kommt aus dem Suite-SSO. Eine **hostgebundene Brücke** in `core/routing.ts` führt `/login?code=` auf dem uav-Host ins Modul statt in den SSO-Login.

**Tech Stack:** Next.js 16 (App Router, RSC, Route Handler) · Drizzle + better-sqlite3 · Auth.js v5 (nur Admin) · Ant Design 6 (nur Verwaltung) · CSS Modules (Teilnehmer) · Vitest + Playwright · pnpm · Service Worker als Route Handler (Muster `qr`, `radio`)

**Spec:** `docs/superpowers/specs/2026-08-28-uav-modul-design.md` — dort steht das **Warum**. Dieser Plan ist das **Wie**. Die Alt-Anwendung liegt unter `/Users/rubeen/dev/personal/drk/uav-praxis` (Freeze-SHA `cd973b0`); jede `datei:zeile`-Angabe über sie meint diesen Stand.

## Global Constraints

Jede gilt für **jede** Aufgabe. Keine davon findet `pnpm build`.

- **Die vier Zugänge sind unantastbar** (Spec §3): Cookie-Name `sid` · `path=/` · `httpOnly` · `SameSite=Lax` · **kein** `domain`-Attribut · DB hält `sha256(rohToken)` als Hex · Code-Alphabet `0123456789ABCDEFGHJKMNPQRSTVWXYZ` · Normalisierung trim→upper→`[\s-]`→I/L→1, O→0, U→V · `localStorage`-Keys `drk-drohnen-fortschritt`, `drk-drohnen-katalog`, `drk-drohnen-sync-queue`, `drk-drohnen-last-sync`, `drk-drohnen-uebernommen` · Queue-Format `{art:'execution'|'taskStatus', daten}`.
- **Spaltennamen und Typen der fünf Tabellen sind die der Alt-Anwendung** (`participants`, `tasks`, `executions`, `task_status`, `sessions`; `schema.sql:16-75`). Zeitstempel sind ISO-8601-**Text** — bewusste Abweichung von der Suite-Regel „`{ mode: "timestamp" }`", weil der Import byte-gleich kopiert und der Paritätscheck sonst nie grün wird. Keine neuen Spalten.
- **Zwei Design-Klassen** (`docs/design/README.md`): Teilnehmer-Ansichten sind **öffentliche Ansichten** — eigene CSS-Module, **kein antd**, keine antd-Importe in `_ui/teilnehmer/`. Verwaltung ist antd + Suite-Chrome.
- **Kein Compound-Zugriff auf antd in einer Server Component** (`Typography.*`, `Form.Item`, `Table.Column`, …). `@ant-design/icons` kommt im Modul nicht vor; Ikonen aus `react-icons/pi`.
- **Werte, die eine Server Component oder ein Route Handler liest, liegen in `_lib/` ohne `"use client"`** (Falle 6). Der SW-Quelltext, Cookie-Namen, Konstanten: `_lib/`.
- **`href` trägt die äußere Pfadform** (`/aufgabe?id=1-1`, nicht `/m/uav/aufgabe`). API-Aufrufe der Client-Insel gehen auf `/api/...` (äußere Form) — der Host-Rewrite bildet sie auf `/m/uav/api/...` ab.
- **Jeder Route Handler unter `api/` und `sw.js/` beginnt mit dem Host-Riegel** `hostAbweisung(req)` (Task 5). Handler haben kein Layout — das ist die Stelle, die man vergisst (`files/_lib/hostRolle.ts`).
- **Admin-Gating** über `isModuleAdmin(getModule("uav"), groups)` aus `@/core/groups` — nie das Registry-Feld direkt. Seiten → `notFound()`, Server Actions → `throw`, Route Handler → `403`.
- **Rate-Limit pro Code, nicht pro IP** (Spec §4; hinter Cloudflare zählt die IP gegen einen Sammel-Eimer). `RateLimiter` aus `@/core/ratelimit`.
- **Tests, die Verhalten sichern, werden per Mutation geprüft** (Guard entfernen → rot). Ein Idempotenztest ändert zwischen zwei Läufen eine Zeile.
- **Commit-Sprache Deutsch, Präfix `uav:`**, Co-Author-Zeilen wie im Repo üblich. Nach jeder Aufgabe: `pnpm typecheck && pnpm lint && pnpm vitest run <betroffene Dateien>`.
- **Nichts aus `uav-praxis` wird per `import` referenziert.** Portieren heißt kopieren + anpassen; die Quelle bleibt eingefroren.

## Dateistruktur (Zielbild)

```
src/app/m/uav/
  layout.tsx                      Minimal-Shell + Manifest-Link + RegisterSW (Teilnehmer)
  page.tsx                        Startseite (Insel: Dashboard)
  aufgabe/page.tsx                Aufgabe (Insel liest ?id= aus der Adresszeile)
  aufgabe/[id]/route.ts           308 → /aufgabe?id=<id> (Alt-Bookmarks)
  login/page.tsx                  Code-Eingabe; ?code= wird sofort eingelöst
  (admin)/layout.tsx              Voll-Shell + requireUavAdminPage
  (admin)/admin/page.tsx          Teilnehmerliste
  (admin)/admin/teilnehmer/[id]/page.tsx
  (admin)/admin/katalog/page.tsx
  _actions/teilnehmer.ts, _actions/katalog.ts
  _db/schema.ts, client.ts, drizzle.config.ts, migrations/
  _lib/code.ts sitzung.ts host.ts hostRiegel.ts requireUavAdmin.ts queries.ts
       csv.ts magicLink.ts sw-quelle.ts boot.ts seedLokal.ts typen.ts
  _ui/teilnehmer/  TeilnehmerApp.tsx Dashboard.tsx TaskDetail.tsx DurchfuehrungForm.tsx
                   AnzahlFeld.tsx TaskCard.tsx LoginForm.tsx uav.module.css
  _ui/offline/     localStore.ts syncEngine.ts client.ts progress.ts useFortschritt.ts useKatalog.ts
  _ui/admin/       TeilnehmerTabelle.tsx TeilnehmerFormular.tsx KatalogTabelle.tsx AufgabeFormular.tsx
  api/anmeldung/route.ts  api/abmeldung/route.ts  api/me/route.ts
  api/tasks/route.ts  api/progress/route.ts  api/sync/route.ts
  api/admin/participants/route.ts  api/admin/participants/export/route.ts
  api/admin/participants/[id]/route.ts  api/admin/participants/[id]/export/route.ts
  api/admin/tasks/route.ts  api/admin/tasks/[id]/route.ts  api/admin/tasks/reorder/route.ts
  sw.js/route.ts  manifest.webmanifest/route.ts  pwa-icon.svg/route.ts  RegisterSW.tsx
public/m/uav/illustrations/*.webp
scripts/import/uav.ts (+ .test.ts)
e2e/uav.spec.ts, e2e/helpers/uav.ts
docs/runbooks/uav-cutover.md
```

---

## Phase A — Fundament

### Task 1: Registry, Env, Dockerfile, Migrations-Dreieck

**Files:**
- Modify: `src/core/registry.ts` (MODULES, vor `alpha`)
- Modify: `src/core/bootstrap.ts:22-59` (MODULE_MIGRATIONS)
- Modify: `Dockerfile:51-57` (COPY-Zeile)
- Modify: `.env.example` (nach den `SUITE_ADMIN_GROUP_RADIO`-Zeilen)
- Create: `src/app/m/uav/_db/migrations/.gitkeep` (Task 2 füllt den Ordner)
- Test: `src/app/m/uav/registry.test.ts`

**Interfaces:**
- Produces: Modul-Key `"uav"`, `getModule("uav")` liefert `shell: "minimal"`, `adminGroups: ["uav-training-admin"]`.

- [ ] **Step 1: Test schreiben**

```ts
// src/app/m/uav/registry.test.ts
import { describe, it, expect } from "vitest";
import { getModule, moduleForHost, canAccess } from "@/core/registry";
import { isModuleAdmin } from "@/core/groups";

describe("Registry-Eintrag uav", () => {
  it("ist ein anonymes Minimal-Shell-Modul mit der Admin-Gruppe uav-training-admin", () => {
    const m = getModule("uav");
    expect(m.shell).toBe("minimal");
    expect(m.requiresAuth).toBe(false);
    expect(m.requiredGroups).toEqual([]);
    expect(m.adminGroups).toEqual(["uav-training-admin"]);
    expect(m.showInSwitcher).toBe(true);
    expect(m.switcherGroupSources).toEqual(["admin"]);
  });
  it("wird über SUITE_HOST_UAV gefunden und ist anonym begehbar", () => {
    const m = moduleForHost("uav-training.iuk-ue.de", { SUITE_HOST_UAV: "uav-training.iuk-ue.de" });
    expect(m?.key).toBe("uav");
    expect(canAccess(getModule("uav"), null)).toBe(true);
  });
  it("Admin nur mit Gruppe — und SUITE_ADMIN_GROUP_UAV gewinnt", () => {
    const m = getModule("uav");
    expect(isModuleAdmin(m, ["uav-training-admin"], {})).toBe(true);
    expect(isModuleAdmin(m, ["irgendwas"], {})).toBe(false);
    expect(isModuleAdmin(m, ["andere"], { SUITE_ADMIN_GROUP_UAV: "andere" })).toBe(true);
  });
});
```

- [ ] **Step 2: Test laufen lassen — rot**

Run: `pnpm vitest run src/app/m/uav/registry.test.ts`
Expected: FAIL `Unknown module: uav`

- [ ] **Step 3: Registry-Eintrag, Migrations-Liste, Dockerfile, .env.example**

In `src/core/registry.ts` vor dem `alpha`-Eintrag:

```ts
  // uav: Teilnehmer melden sich mit einem Dauer-Code an (kein SSO) → requiresAuth:false.
  // Die Verwaltung gated `_lib/requireUavAdmin.ts` (Layout UND jeder Handler unter api/admin/).
  // Vorgabe uav-training-admin (Betreiber, 28.08.2026); Instanzwert über SUITE_ADMIN_GROUP_UAV.
  { key: "uav", title: "Drohnentraining", icon: "RocketOutlined", shell: "minimal",
    requiresAuth: false, requiredGroups: [], adminGroups: ["uav-training-admin"],
    prodHosts: [], showInSwitcher: true, switcherGroupSources: ["admin"] },
```

In `src/core/bootstrap.ts` nach dem `radio`-Eintrag von `MODULE_MIGRATIONS`:

```ts
  // uav: OHNE Schema-Import und OHNE Boot-Seed — ein geseedeter Teilnehmer-Code wäre in einer
  // Generalprobe (SUITE_SEED=1) ein gültiger anonymer Zugang. Das lokale Seed-Skript deckt Dev ab.
  { key: "uav", migrationsFolder: "src/app/m/uav/_db/migrations" },
```

Im `Dockerfile` nach Zeile 57:

```dockerfile
COPY --from=builder --chown=nextjs:nodejs /app/src/app/m/uav/_db/migrations ./src/app/m/uav/_db/migrations
```

In `.env.example` nach dem `SUITE_ADMIN_GROUP_RADIO`-Block:

```dotenv
# uav (Drohnentraining): Verwaltung nur für diese Gruppe. Leer gesetzt = nur der Suite-Admin.
# SUITE_ADMIN_GROUP_UAV=uav-training-admin
# Service-Worker-Modus des Moduls (Spec §5): `abraeumen` liefert unter /sw.js den Worker, der den
# Alt-Workbox-Worker ersetzt und sich austrägt — Pflicht vom ersten Deploy bis Ende Standby.
# `cachen` liefert den Allowlist-Worker für Offline. Fehlt die Variable bei gesetztem
# SUITE_HOST_UAV, bricht der Boot ab (`_lib/boot.ts`).
# UAV_SW_MODUS=abraeumen
```

Bei `SUITE_HOST_*` (Zeile ~151): `# SUITE_HOST_UAV=` ergänzen.

- [ ] **Step 4: Tests grün, Dreieck-Test grün**

Run: `pnpm vitest run src/app/m/uav/registry.test.ts src/core/bootstrap.test.ts src/core/registry.test.ts scripts/seed-lokal.test.ts`
Expected: `registry.test.ts` PASS; `bootstrap.test.ts` PASS erst, wenn `migrations/` existiert (leerer Ordner mit `.gitkeep` reicht für das Dreieck; der Seed-Test verlangt einen `seedLokal` — er wird in Task 20 grün, bis dahin darf er rot sein und wird im Commit-Text genannt).

- [ ] **Step 5: Commit**

```bash
git add src/core/registry.ts src/core/bootstrap.ts Dockerfile .env.example src/app/m/uav
git commit -m "uav: Registry-Eintrag, Migrations-Dreieck und Env-Zeilen für das Modul Drohnentraining"
```

### Task 2: Drizzle-Schema 1:1 und Migration 0000

**Files:**
- Create: `src/app/m/uav/_db/schema.ts`, `_db/client.ts`, `_db/drizzle.config.ts`
- Create (generiert): `src/app/m/uav/_db/migrations/0000_*.sql` + `meta/`
- Test: `src/app/m/uav/_db/migrations.test.ts`

**Interfaces:**
- Produces: `getDb(): BetterSQLite3Database<typeof schema>`; Tabellen `participants`, `tasks`, `executions`, `taskStatus` (SQL `task_status`), `sessions`; Row-Typen `ParticipantRow`, `TaskRow`, `ExecutionRow`, `TaskStatusRow`, `SessionRow`.

- [ ] **Step 1: Test — Spaltennamen müssen exakt die der Alt-Anwendung sein**

```ts
// src/app/m/uav/_db/migrations.test.ts
import { it, expect, beforeAll, afterAll } from "vitest";
import Database from "better-sqlite3";
import { rmSync } from "node:fs";
import { migrateAllModules } from "@/core/bootstrap";

const DIR = "./.data/uav-migrations-test";
beforeAll(() => { rmSync(DIR, { recursive: true, force: true }); process.env.DATA_DIR = DIR; migrateAllModules(); });
afterAll(() => rmSync(DIR, { recursive: true, force: true }));

function spalten(tabelle: string): string[] {
  const db = new Database(`${DIR}/uav.db`, { readonly: true });
  try {
    return (db.prepare(`PRAGMA table_info(${tabelle})`).all() as { name: string }[]).map((c) => c.name);
  } finally { db.close(); }
}

// Reihenfolge und Namen aus uav-praxis/server/db/schema.sql — der Import kopiert per
// INSERT ... SELECT * und der Paritätscheck hasht Zeilen; jede Abweichung wäre ein roter Cutover.
it.each([
  ["participants", ["id", "name", "login_code", "aktiv", "beginn", "created_at", "last_seen"]],
  ["tasks", ["id", "teil", "nummer", "titel", "lernziel", "schritte", "durchfuehrungshinweise",
             "sicherheitshinweise", "zielanzahl_default", "sort_order", "aktiv", "bild", "updated_at"]],
  ["executions", ["id", "participant_id", "task_id", "datum", "drohnensteuerer", "luftraumbeobachter",
                  "created_at", "deleted_at"]],
  ["task_status", ["participant_id", "task_id", "zielanzahl", "nicht_anwendbar", "updated_at"]],
  ["sessions", ["token", "kind", "subject_id", "created_at", "expires_at"]],
])("%s trägt die Alt-Spalten in Alt-Reihenfolge", (tabelle, erwartet) => {
  expect(spalten(tabelle)).toEqual(erwartet);
});

it("hat weder admins noch oidc_states", () => {
  const db = new Database(`${DIR}/uav.db`, { readonly: true });
  const namen = (db.prepare("select name from sqlite_master where type='table'").all() as { name: string }[]).map((r) => r.name);
  db.close();
  expect(namen).not.toContain("admins");
  expect(namen).not.toContain("oidc_states");
});
```

- [ ] **Step 2: rot**

Run: `pnpm vitest run src/app/m/uav/_db/migrations.test.ts` → FAIL (keine Tabellen).

- [ ] **Step 3: Schema, Client, Config**

```ts
// src/app/m/uav/_db/schema.ts
import { sqliteTable, text, integer, index, primaryKey } from "drizzle-orm/sqlite-core";

/*
 * 1:1 aus uav-praxis/server/db/schema.sql (Spec §2). Zeitstempel sind ISO-8601-TEXT (UTC),
 * Kalendertage ISO yyyy-mm-dd — der Import kopiert Werte unverändert.
 */
export const participants = sqliteTable("participants", {
  id: text("id").primaryKey(),
  name: text("name").notNull(),
  loginCode: text("login_code").notNull().unique(),      // Crockford-Base32, 8 Zeichen
  aktiv: integer("aktiv").notNull().default(1),
  beginn: text("beginn"),
  createdAt: text("created_at").notNull(),
  lastSeen: text("last_seen"),
});

export const tasks = sqliteTable("tasks", {
  id: text("id").primaryKey(),                            // "1-1" beim Seed, sonst UUID
  teil: integer("teil").notNull(),
  nummer: text("nummer").notNull(),
  titel: text("titel").notNull(),
  lernziel: text("lernziel").notNull().default(""),
  schritte: text("schritte").notNull().default("[]"),     // JSON string[]
  durchfuehrungshinweise: text("durchfuehrungshinweise").notNull().default("[]"),
  sicherheitshinweise: text("sicherheitshinweise").notNull().default("[]"),
  zielanzahlDefault: integer("zielanzahl_default").notNull().default(1),
  sortOrder: integer("sort_order").notNull().default(0),
  aktiv: integer("aktiv").notNull().default(1),
  bild: text("bild"),                                     // relativer Pfad, Task 21 setzt das Präfix
  updatedAt: text("updated_at").notNull(),
});

export const executions = sqliteTable(
  "executions",
  {
    id: text("id").primaryKey(),                          // CLIENT-UUID → Idempotenz des Sync
    participantId: text("participant_id").notNull().references(() => participants.id, { onDelete: "cascade" }),
    taskId: text("task_id").notNull(),
    datum: text("datum").notNull(),
    drohnensteuerer: text("drohnensteuerer").notNull().default(""),
    luftraumbeobachter: text("luftraumbeobachter").notNull().default(""),
    createdAt: text("created_at").notNull(),
    deletedAt: text("deleted_at"),                        // Tombstone
  },
  (t) => [index("idx_executions_participant").on(t.participantId)],
);

export const taskStatus = sqliteTable(
  "task_status",
  {
    participantId: text("participant_id").notNull().references(() => participants.id, { onDelete: "cascade" }),
    taskId: text("task_id").notNull(),
    zielanzahl: integer("zielanzahl"),
    nichtAnwendbar: integer("nicht_anwendbar").notNull().default(0),
    updatedAt: text("updated_at").notNull(),
  },
  (t) => [primaryKey({ columns: [t.participantId, t.taskId] })],
);

export const sessions = sqliteTable("sessions", {
  token: text("token").primaryKey(),                      // sha256-hex des Roh-Tokens
  kind: text("kind").notNull(),                           // nur noch 'participant'; 'admin' bleibt importierbar, wird nie gelesen
  subjectId: text("subject_id").notNull(),
  createdAt: text("created_at").notNull(),
  expiresAt: text("expires_at").notNull(),
});

export type ParticipantRow = typeof participants.$inferSelect;
export type TaskRow = typeof tasks.$inferSelect;
export type ExecutionRow = typeof executions.$inferSelect;
export type TaskStatusRow = typeof taskStatus.$inferSelect;
export type SessionRow = typeof sessions.$inferSelect;
```

```ts
// src/app/m/uav/_db/client.ts
import { getModuleDb } from "@/core/db";
import * as schema from "./schema";
export const getDb = () => getModuleDb("uav", schema);
export type UavDb = ReturnType<typeof getDb>;
```

```ts
// src/app/m/uav/_db/drizzle.config.ts
import type { Config } from "drizzle-kit";
export default {
  schema: "./src/app/m/uav/_db/schema.ts",
  out: "./src/app/m/uav/_db/migrations",
  dialect: "sqlite",
  dbCredentials: { url: "./.data/uav.db" },
} satisfies Config;
```

Migration erzeugen: `pnpm drizzle-kit generate --config src/app/m/uav/_db/drizzle.config.ts`. `.gitkeep` löschen. Die erzeugte SQL prüfen: `login_code` muss `UNIQUE` tragen, `task_status` einen zusammengesetzten PK.

- [ ] **Step 4: grün**

Run: `pnpm vitest run src/app/m/uav/_db/migrations.test.ts src/core/bootstrap.test.ts` → PASS.

- [ ] **Step 5: Commit**

```bash
git add src/app/m/uav/_db
git commit -m "uav: Drizzle-Schema 1:1 aus der Alt-Anwendung und Migration 0000"
```

### Task 3: Login-Codes — Alphabet und Normalisierung abgeschrieben

**Files:**
- Create: `src/app/m/uav/_lib/code.ts`
- Test: `src/app/m/uav/_lib/code.test.ts`

**Interfaces:**
- Produces: `CODE_ALPHABET`, `loginCodeErzeugen(): string`, `codeNormalisieren(eingabe: string): string`.

- [ ] **Step 1: Test mit Fixture-Paaren (aus `uav-praxis/server/auth/codes.ts:4,24-31` abgeleitet)**

```ts
// src/app/m/uav/_lib/code.test.ts
import { describe, it, expect } from "vitest";
import { CODE_ALPHABET, codeNormalisieren, loginCodeErzeugen } from "./code";

describe("codeNormalisieren — identisch zur Alt-Anwendung", () => {
  it.each([
    ["  abcd-efgh ", "ABCDEFGH"],
    ["il1o0uv", "1110 0VV".replace(" ", "")],   // I→1, L→1, O→0, U→V
    ["A B\tC\nD", "ABCD"],
    ["x-y-z", "XYZ"],
    ["", ""],
  ])("%j → %j", (rein, raus) => expect(codeNormalisieren(rein)).toBe(raus));
});

describe("loginCodeErzeugen", () => {
  it("liefert 8 Zeichen aus dem Crockford-Alphabet ohne I, L, O, U", () => {
    expect(CODE_ALPHABET).toBe("0123456789ABCDEFGHJKMNPQRSTVWXYZ");
    for (let i = 0; i < 200; i++) {
      const c = loginCodeErzeugen();
      expect(c).toHaveLength(8);
      for (const z of c) expect(CODE_ALPHABET).toContain(z);
    }
  });
  it("ist ein Fixpunkt der Normalisierung", () => {
    for (let i = 0; i < 50; i++) { const c = loginCodeErzeugen(); expect(codeNormalisieren(c)).toBe(c); }
  });
});
```

- [ ] **Step 2: rot** — `pnpm vitest run src/app/m/uav/_lib/code.test.ts`

- [ ] **Step 3: Implementierung**

```ts
// src/app/m/uav/_lib/code.ts
import { randomInt } from "node:crypto";

/** Crockford-Base32 ohne mehrdeutige Zeichen — WÖRTLICH aus uav-praxis/server/auth/codes.ts:4. */
export const CODE_ALPHABET = "0123456789ABCDEFGHJKMNPQRSTVWXYZ";

export function loginCodeErzeugen(): string {
  let code = "";
  for (let i = 0; i < 8; i++) code += CODE_ALPHABET[randomInt(0, CODE_ALPHABET.length)];
  return code;
}

/** Abgeschrieben aus codes.ts:24-31 — verteilte Codes auf Zetteln hängen an genau dieser Abbildung. */
export function codeNormalisieren(code: string): string {
  return code.trim().toUpperCase().replace(/[\s-]/g, "").replace(/[IL]/g, "1").replace(/O/g, "0").replace(/U/g, "V");
}
```

- [ ] **Step 4: grün** · **Step 5: Commit** `uav: Login-Code-Alphabet und Normalisierung aus der Alt-Anwendung übernommen`

### Task 4: Teilnehmer-Session — Cookie `sid`, SHA-256, 180 Tage

**Files:**
- Create: `src/app/m/uav/_lib/sitzung.ts`
- Test: `src/app/m/uav/_lib/sitzung.test.ts`

**Interfaces:**
- Produces: `SID_COOKIE = "sid"`, `TEILNEHMER_TTL_MS`, `tokenHash(roh): string`, `sessionErzeugen(db, participantId): string` (Roh-Token), `sessionValidieren(db, roh): TeilnehmerIdentity | null`, `sessionLoeschen(db, roh)`, `sidCookieOptionen()`, `type Identity = {kind:'anon'} | {kind:'participant'; id; name} | {kind:'admin'; id; name; email}`.

- [ ] **Step 1: Test — inkl. eines echten Alt-Paars**

Das Fixture-Paar entsteht einmal mit `node -e "const c=require('crypto');const t='alt-fixture-roh-token-0001';console.log(t, c.createHash('sha256').update(t).digest('hex'))"` — den Hex-Wert in den Test schreiben (er ist deterministisch, das ist der Punkt: die neue Implementierung muss **denselben** Hash liefern wie `sessions.ts:39`).

```ts
// src/app/m/uav/_lib/sitzung.test.ts
import { describe, it, expect, beforeEach } from "vitest";
import { createHash } from "node:crypto";
import { testDb } from "./testDb";
import { SID_COOKIE, sessionErzeugen, sessionValidieren, sessionLoeschen, sidCookieOptionen, tokenHash } from "./sitzung";
import { participants, sessions } from "../_db/schema";

const ROH = "alt-fixture-roh-token-0001";
const HEX = createHash("sha256").update(ROH).digest("hex"); // = Alt-Hash (sessions.ts:39)

describe("Teilnehmer-Session", () => {
  let db: ReturnType<typeof testDb>;
  beforeEach(() => {
    db = testDb();
    db.insert(participants).values({ id: "p1", name: "Ada", loginCode: "ABCDEFGH", aktiv: 1, createdAt: "2026-01-01T00:00:00.000Z" }).run();
  });
  it("heißt sid, path=/, httpOnly, lax, ohne domain", () => {
    expect(SID_COOKIE).toBe("sid");
    const o = sidCookieOptionen();
    expect(o).toMatchObject({ path: "/", httpOnly: true, sameSite: "lax", maxAge: 180 * 86400 });
    expect("domain" in o).toBe(false);
  });
  it("erkennt eine importierte Alt-Session am Alt-Hash", () => {
    db.insert(sessions).values({ token: HEX, kind: "participant", subjectId: "p1", createdAt: "2026-08-01T00:00:00.000Z", expiresAt: "2099-01-01T00:00:00.000Z" }).run();
    expect(tokenHash(ROH)).toBe(HEX);
    expect(sessionValidieren(db, ROH)).toEqual({ kind: "participant", id: "p1", name: "Ada" });
  });
  it("neue Session: Roh-Token im Rückgabewert, Hash in der Tabelle, 180 Tage", () => {
    const roh = sessionErzeugen(db, "p1");
    const zeile = db.select().from(sessions).all()[0];
    expect(zeile.token).toBe(tokenHash(roh));
    expect(zeile.token).not.toBe(roh);
    const ttl = new Date(zeile.expiresAt).getTime() - new Date(zeile.createdAt).getTime();
    expect(ttl).toBe(180 * 86400 * 1000);
  });
  it("abgelaufen → null und die Zeile ist weg; inaktiver Teilnehmer → null; admin-kind → null", () => {
    db.insert(sessions).values({ token: tokenHash("alt"), kind: "participant", subjectId: "p1", createdAt: "2020-01-01T00:00:00.000Z", expiresAt: "2020-01-02T00:00:00.000Z" }).run();
    expect(sessionValidieren(db, "alt")).toBeNull();
    expect(db.select().from(sessions).all()).toHaveLength(0);
    const roh = sessionErzeugen(db, "p1");
    db.update(participants).set({ aktiv: 0 }).run();
    expect(sessionValidieren(db, roh)).toBeNull();
    db.insert(sessions).values({ token: tokenHash("adm"), kind: "admin", subjectId: "x", createdAt: "2026-01-01T00:00:00.000Z", expiresAt: "2099-01-01T00:00:00.000Z" }).run();
    expect(sessionValidieren(db, "adm")).toBeNull();
  });
  it("löschen entfernt genau diese Session", () => {
    const a = sessionErzeugen(db, "p1"); const b = sessionErzeugen(db, "p1");
    sessionLoeschen(db, a);
    expect(sessionValidieren(db, a)).toBeNull();
    expect(sessionValidieren(db, b)).not.toBeNull();
  });
});
```

Dazu der Test-Helfer (einmal, von allen `_lib`-Tests benutzt):

```ts
// src/app/m/uav/_lib/testDb.ts
import { drizzle } from "drizzle-orm/better-sqlite3";
import { migrate } from "drizzle-orm/better-sqlite3/migrator";
import { openModuleDatabase } from "@/core/db";
import * as schema from "../_db/schema";

/** Frische In-Memory-DB mit den echten Migrationen — jeder Test bekommt seine eigene. */
export function testDb() {
  const db = drizzle(openModuleDatabase(":memory:"), { schema });
  migrate(db, { migrationsFolder: "src/app/m/uav/_db/migrations" });
  return db;
}
export type TestDb = ReturnType<typeof testDb>;
```

- [ ] **Step 2: rot** · **Step 3: Implementierung**

```ts
// src/app/m/uav/_lib/sitzung.ts
import { createHash, randomBytes, timingSafeEqual } from "node:crypto";
import { and, eq } from "drizzle-orm";
import type { UavDb } from "../_db/client";
import { participants, sessions } from "../_db/schema";

export const SID_COOKIE = "sid";                        // Alt: sessions.ts:10
export const TEILNEHMER_TTL_MS = 180 * 24 * 60 * 60 * 1000; // Alt: sessions.ts:13

export type Identity =
  | { kind: "anon" }
  | { kind: "participant"; id: string; name: string }
  | { kind: "admin"; id: string; name: string | null; email: string | null };

export function tokenHash(roh: string): string {
  return createHash("sha256").update(roh).digest("hex");
}

export function sessionErzeugen(db: UavDb, participantId: string): string {
  const roh = randomBytes(32).toString("base64url");
  const jetzt = Date.now();
  db.insert(sessions).values({
    token: tokenHash(roh), kind: "participant", subjectId: participantId,
    createdAt: new Date(jetzt).toISOString(), expiresAt: new Date(jetzt + TEILNEHMER_TTL_MS).toISOString(),
  }).run();
  return roh;
}

export function sessionValidieren(db: UavDb, roh: string): Identity | null {
  if (!roh) return null;
  const hash = tokenHash(roh);
  const zeile = db.select().from(sessions).where(eq(sessions.token, hash)).get();
  if (!zeile) return null;
  // Zeitkonstanter Vergleich des gefundenen Hashs gegen den berechneten — der Index-Lookup ist
  // nicht zeitkonstant, aber der Hash ist es, was hier verglichen wird (Spec §3, Zeile 3).
  if (!timingSafeEqual(Buffer.from(zeile.token), Buffer.from(hash))) return null;
  if (new Date(zeile.expiresAt).getTime() <= Date.now()) {
    db.delete(sessions).where(eq(sessions.token, hash)).run();
    return null;
  }
  if (zeile.kind !== "participant") return null;      // Alt-Admin-Sessions verfallen bewusst (Spec §2)
  const p = db.select({ id: participants.id, name: participants.name }).from(participants)
    .where(and(eq(participants.id, zeile.subjectId), eq(participants.aktiv, 1))).get();
  return p ? { kind: "participant", id: p.id, name: p.name } : null;
}

export function sessionLoeschen(db: UavDb, roh: string): void {
  if (roh) db.delete(sessions).where(eq(sessions.token, tokenHash(roh))).run();
}

export function sidCookieOptionen(): { httpOnly: true; sameSite: "lax"; path: "/"; secure: boolean; maxAge: number } {
  return { httpOnly: true, sameSite: "lax", path: "/", secure: process.env.NODE_ENV === "production", maxAge: TEILNEHMER_TTL_MS / 1000 };
}
```

- [ ] **Step 4: grün** · **Step 5: Mutation:** in `sessionValidieren` die Zeile `if (zeile.kind !== "participant") return null;` entfernen → Test „admin-kind → null" muss rot werden; zurücknehmen. **Step 6: Commit** `uav: Teilnehmer-Session mit Alt-Cookie sid und Alt-Hash`

### Task 5: Host-Riegel des Moduls

**Files:**
- Create: `src/app/m/uav/_lib/host.ts`, `_lib/hostRiegel.ts`
- Test: `src/app/m/uav/_lib/hostRiegel.test.ts`

**Interfaces:**
- Produces: `istUavHost(headers: Headers): boolean`, `requireUavHost(headers): void` (notFound), `hostAbweisung(req: Request): Response | null`.

- [ ] **Step 1: Test**

```ts
// src/app/m/uav/_lib/hostRiegel.test.ts
import { describe, it, expect, beforeEach, afterEach } from "vitest";
import { hostAbweisung } from "./hostRiegel";
import { istUavHost } from "./host";

const alt = process.env.SUITE_HOST_UAV;
beforeEach(() => { process.env.SUITE_HOST_UAV = "uav-training.iuk-ue.de"; });
afterEach(() => { if (alt === undefined) delete process.env.SUITE_HOST_UAV; else process.env.SUITE_HOST_UAV = alt; });

const req = (host: string) => new Request("http://x/api/me", { headers: { host } });

describe("uav-Host-Riegel", () => {
  it("Prod-Host und Dev-Host sind eigen", () => {
    expect(istUavHost(new Headers({ host: "uav-training.iuk-ue.de" }))).toBe(true);
    expect(istUavHost(new Headers({ host: "uav.localtest.me:3000" }))).toBe(true);
  });
  it("fremder Suite-Host → 404 als text, nicht HTML", async () => {
    const r = hostAbweisung(req("feedback.localtest.me"));
    expect(r?.status).toBe(404);
    expect(r?.headers.get("content-type")).not.toContain("text/html");
  });
  it("eigener Host → null", () => expect(hostAbweisung(req("uav-training.iuk-ue.de"))).toBeNull());
  it("x-forwarded-host gewinnt", () => {
    expect(hostAbweisung(new Request("http://x/", { headers: { host: "localhost:3000", "x-forwarded-host": "uav-training.iuk-ue.de" } }))).toBeNull();
  });
});
```

- [ ] **Step 2: rot** · **Step 3: Implementierung** (Muster `radio/_lib/host.ts`, `hostRiegel.ts`)

```ts
// src/app/m/uav/_lib/host.ts
import { notFound } from "next/navigation";
import { moduleForHost } from "@/core/registry";
import { resolveHost } from "@/core/routing";
export function istUavHost(headers: Headers): boolean { return moduleForHost(resolveHost(headers))?.key === "uav"; }
export function requireUavHost(headers: Headers): void { if (!istUavHost(headers)) notFound(); }
```

```ts
// src/app/m/uav/_lib/hostRiegel.ts
import { istUavHost } from "./host";
/** ERSTE Anweisung jedes Route Handlers unter api/ und sw.js/ — Handler haben kein Layout. */
export function hostAbweisung(req: Request): Response | null {
  return istUavHost(new Headers(req.headers)) ? null : new Response("Not found", { status: 404 });
}
```

- [ ] **Step 4: grün** · **Step 5: Commit** `uav: Host-Riegel für Route Handler`

### Task 6: Datenzugriff — `queries.ts` (Port von `repo.ts`)

**Files:**
- Create: `src/app/m/uav/_lib/typen.ts` (DTOs aus `uav-praxis/shared/types.ts`, 1:1 kopieren, Exporte unverändert)
- Create: `src/app/m/uav/_lib/queries.ts`
- Test: `src/app/m/uav/_lib/queries.test.ts`

**Interfaces:**
- Produces (alle nehmen `db: UavDb` als ersten Parameter):
  `alleTasks(db, inklusiveInaktiv=false): TaskDTO[]` · `taskById(db,id)` · `taskAnlegen(db, eingabe)` · `taskAendern(db,id,patch)` · `taskLoeschen(db,id)` · `tasksNeuSortieren(db, ids)` ·
  `alleTeilnehmer(db)` · `teilnehmerById(db,id)` · `teilnehmerAnlegen(db, name, beginn)` · `teilnehmerAendern(db,id,patch&{codeNeu?})` · `teilnehmerLoeschen(db,id)` · `teilnehmerPerCode(db, normalisierterCode)` · `teilnehmerGesehen(db,id)` · `eindeutigenCodeErzeugen(db)` ·
  `fortschritt(db, participantId): ProgressSnapshot` · `sync(db, participantId, req: SyncRequest): ProgressSnapshot` · `teilnehmerUebersicht(db): ParticipantProgressDTO[]` · `teilnehmerDetail(db,id): ParticipantDetailDTO` · `class NotFound extends Error`.
  Semantik wörtlich aus `uav-praxis/server/db/repo.ts` (Zeilen 1-445 im kommentarfreien Auszug): `sync` = Executions-Upsert per `id` mit allen Feldern inkl. `deleted_at`; TaskStatus-Upsert nur `WHERE excluded.updated_at > task_status.updated_at`; `created_at` der Execution = Serverzeit des Sync.

- [ ] **Step 1: Tests — Sync-Semantik per Mutation absicherbar**

```ts
// src/app/m/uav/_lib/queries.test.ts
import { describe, it, expect, beforeEach } from "vitest";
import { testDb, type TestDb } from "./testDb";
import * as q from "./queries";
import { executions, taskStatus } from "../_db/schema";

function grund(db: TestDb) {
  const p = q.teilnehmerAnlegen(db, "Ada", "2026-08-01");
  q.taskAnlegen(db, { id: "1-1", teil: 1, nummer: "1.1", titel: "Start", lernziel: "", schritte: [], durchfuehrungshinweise: [], sicherheitshinweise: [], zielanzahlDefault: 2, aktiv: true });
  q.taskAnlegen(db, { id: "1-2", teil: 1, nummer: "1.2", titel: "Landung", lernziel: "", schritte: [], durchfuehrungshinweise: [], sicherheitshinweise: [], zielanzahlDefault: 1, aktiv: true });
  return p;
}

describe("sync", () => {
  let db: TestDb; let pid: string;
  beforeEach(() => { db = testDb(); pid = grund(db).id; });

  it("ist idempotent per Client-UUID — und ein zweiter Lauf mit geänderten Feldern ÄNDERT die Zeile", () => {
    const e = { id: "e1", taskId: "1-1", datum: "2026-08-10", drohnensteuerer: "A", luftraumbeobachter: "B" };
    q.sync(db, pid, { since: null, executions: [e], taskStatus: [] });
    q.sync(db, pid, { since: null, executions: [{ ...e, luftraumbeobachter: "C" }], taskStatus: [] });
    const zeilen = db.select().from(executions).all();
    expect(zeilen).toHaveLength(1);
    expect(zeilen[0].luftraumbeobachter).toBe("C");   // onConflictDoNothing wäre hier rot
  });
  it("Tombstone: deletedAt wird übernommen und fällt aus dem Fortschritt", () => {
    q.sync(db, pid, { since: null, executions: [{ id: "e1", taskId: "1-1", datum: "2026-08-10", drohnensteuerer: "", luftraumbeobachter: "" }], taskStatus: [] });
    q.sync(db, pid, { since: null, executions: [{ id: "e1", taskId: "1-1", datum: "2026-08-10", drohnensteuerer: "", luftraumbeobachter: "", deletedAt: "2026-08-11T00:00:00.000Z" }], taskStatus: [] });
    expect(db.select().from(executions).all()[0].deletedAt).toBe("2026-08-11T00:00:00.000Z");
    expect(q.teilnehmerDetail(db, pid).aufgaben.find((a) => a.taskId === "1-1")?.anzahl).toBe(0);
  });
  it("TaskStatus ist last-write-wins über updatedAt — ein älterer Stand überschreibt nicht", () => {
    q.sync(db, pid, { since: null, executions: [], taskStatus: [{ taskId: "1-1", zielanzahl: 5, nichtAnwendbar: false, updatedAt: "2026-08-10T10:00:00.000Z" }] });
    q.sync(db, pid, { since: null, executions: [], taskStatus: [{ taskId: "1-1", zielanzahl: 3, nichtAnwendbar: false, updatedAt: "2026-08-09T10:00:00.000Z" }] });
    expect(db.select().from(taskStatus).all()[0].zielanzahl).toBe(5);
  });
  it("participantId kommt aus dem Aufruf, nie aus dem Body — fremde Executions landen beim Aufrufer", () => {
    const fremd = q.teilnehmerAnlegen(db, "Bob", null);
    q.sync(db, pid, { since: null, executions: [{ id: "e9", taskId: "1-1", datum: "2026-08-10", drohnensteuerer: "", luftraumbeobachter: "" }], taskStatus: [] });
    expect(db.select().from(executions).all()[0].participantId).toBe(pid);
    expect(q.fortschritt(db, fremd.id).executions).toHaveLength(0);
  });
  it("liefert den vollen Snapshot mit serverTime", () => {
    const s = q.sync(db, pid, { since: null, executions: [], taskStatus: [] });
    expect(s).toEqual({ executions: [], taskStatus: [], serverTime: expect.any(String) });
  });
});

describe("Fortschritt und Auswertung", () => {
  it("erledigt = anzahl >= ziel, nichtAnwendbar zählt nicht; Quote über anwendbare", () => {
    const db = testDb(); const pid = grund(db).id;
    q.sync(db, pid, { since: null,
      executions: [{ id: "a", taskId: "1-1", datum: "2026-08-10", drohnensteuerer: "", luftraumbeobachter: "" },
                   { id: "b", taskId: "1-1", datum: "2026-08-11", drohnensteuerer: "", luftraumbeobachter: "" }],
      taskStatus: [{ taskId: "1-2", zielanzahl: null, nichtAnwendbar: true, updatedAt: "2026-08-10T00:00:00.000Z" }] });
    const d = q.teilnehmerDetail(db, pid);
    expect(d.erledigt).toBe(1); expect(d.gesamt).toBe(1); expect(d.quote).toBe(1);
    expect(d.aufgaben.find((a) => a.taskId === "1-1")?.letzteDurchfuehrung).toBe("2026-08-11");
    expect(d.teile).toEqual([{ teil: 1, erledigt: 1, gesamt: 1, quote: 1 }]);
    expect(q.teilnehmerUebersicht(db)[0]).toMatchObject({ erledigt: 1, gesamt: 1 });
  });
  it("Zielanzahl-Override, mindestens 1", () => {
    const db = testDb(); const pid = grund(db).id;
    q.sync(db, pid, { since: null, executions: [], taskStatus: [{ taskId: "1-1", zielanzahl: 0, nichtAnwendbar: false, updatedAt: "2026-08-10T00:00:00.000Z" }] });
    expect(q.teilnehmerDetail(db, pid).aufgaben[0].ziel).toBe(1);
  });
});

describe("Teilnehmer", () => {
  it("anlegen erzeugt einen eindeutigen 8er-Code; codeNeu rotiert ihn; perCode findet nur aktive", () => {
    const db = testDb();
    const a = q.teilnehmerAnlegen(db, "Ada", null);
    expect(a.loginCode).toMatch(/^[0-9A-HJKMNP-TV-Z]{8}$/);
    expect(q.teilnehmerPerCode(db, a.loginCode)?.id).toBe(a.id);
    const b = q.teilnehmerAendern(db, a.id, { codeNeu: true });
    expect(b.loginCode).not.toBe(a.loginCode);
    q.teilnehmerAendern(db, a.id, { aktiv: false });
    expect(q.teilnehmerPerCode(db, b.loginCode)).toBeNull();
  });
  it("löschen kaskadiert auf executions und task_status", () => {
    const db = testDb(); const pid = grund(db).id;
    q.sync(db, pid, { since: null, executions: [{ id: "e", taskId: "1-1", datum: "2026-08-10", drohnensteuerer: "", luftraumbeobachter: "" }], taskStatus: [{ taskId: "1-1", zielanzahl: 1, nichtAnwendbar: false, updatedAt: "x" }] });
    q.teilnehmerLoeschen(db, pid);
    expect(db.select().from(executions).all()).toHaveLength(0);
    expect(db.select().from(taskStatus).all()).toHaveLength(0);
    expect(() => q.teilnehmerLoeschen(db, pid)).toThrow(q.NotFound);
  });
});

describe("Katalog", () => {
  it("reorder setzt sort_order nach Position; alleTasks(false) filtert inaktive; JSON-Spalten sind Arrays", () => {
    const db = testDb(); grund(db);
    q.tasksNeuSortieren(db, ["1-2", "1-1"]);
    expect(q.alleTasks(db).map((t) => t.id)).toEqual(["1-2", "1-1"]);
    q.taskAendern(db, "1-2", { aktiv: false, schritte: ["a", "b"] });
    expect(q.alleTasks(db).map((t) => t.id)).toEqual(["1-1"]);
    expect(q.alleTasks(db, true).find((t) => t.id === "1-2")?.schritte).toEqual(["a", "b"]);
  });
});
```

- [ ] **Step 2: rot** · **Step 3: Implementierung**

`typen.ts`: Inhalt von `uav-praxis/shared/types.ts` kopieren (alle Interfaces, `Teil`, `Identity` entfällt — der liegt in `sitzung.ts`).

`queries.ts`: Port von `repo.ts` mit Drizzle. Für die zwei Upserts im Sync die Drizzle-Form:

```ts
// Ausschnitt src/app/m/uav/_lib/queries.ts
import { randomUUID } from "node:crypto";
import { and, asc, desc, eq, isNull, sql } from "drizzle-orm";
import type { UavDb } from "../_db/client";
import { executions, participants, sessions, taskStatus, tasks } from "../_db/schema";
import { loginCodeErzeugen } from "./code";
import type { ExecutionDTO, ParticipantDetailDTO, ParticipantDTO, ParticipantProgressDTO, ProgressSnapshot, SyncRequest, TaskDTO, TaskProgressDTO, TaskStatusDTO, Teil, TeilStatDTO } from "./typen";

export class NotFound extends Error { readonly code = "not_found"; constructor(m = "Nicht gefunden") { super(m); this.name = "NotFound"; } }
const jetzt = () => new Date().toISOString();

function parseStringArray(raw: string): string[] { try { const v = JSON.parse(raw); return Array.isArray(v) ? v.map(String) : []; } catch { return []; } }
const jsonArray = (v: string[] | undefined) => JSON.stringify(Array.isArray(v) ? v : []);

function mapTask(r: typeof tasks.$inferSelect): TaskDTO {
  return { id: r.id, teil: r.teil as Teil, nummer: r.nummer, titel: r.titel, lernziel: r.lernziel,
    schritte: parseStringArray(r.schritte), durchfuehrungshinweise: parseStringArray(r.durchfuehrungshinweise),
    sicherheitshinweise: parseStringArray(r.sicherheitshinweise), zielanzahlDefault: r.zielanzahlDefault,
    sortOrder: r.sortOrder, aktiv: r.aktiv === 1, bildUrl: r.bild };
}
function mapParticipant(r: typeof participants.$inferSelect): ParticipantDTO {
  return { id: r.id, name: r.name, loginCode: r.loginCode, aktiv: r.aktiv === 1, beginn: r.beginn, lastSeen: r.lastSeen };
}

export function sync(db: UavDb, participantId: string, req: SyncRequest): ProgressSnapshot {
  const ts = jetzt();
  db.transaction((tx) => {
    for (const e of req.executions) {
      tx.insert(executions).values({
        id: e.id, participantId, taskId: e.taskId, datum: e.datum,
        drohnensteuerer: e.drohnensteuerer ?? "", luftraumbeobachter: e.luftraumbeobachter ?? "",
        createdAt: ts, deletedAt: e.deletedAt ?? null,
      }).onConflictDoUpdate({ target: executions.id, set: {
        taskId: sql`excluded.task_id`, datum: sql`excluded.datum`,
        drohnensteuerer: sql`excluded.drohnensteuerer`, luftraumbeobachter: sql`excluded.luftraumbeobachter`,
        deletedAt: sql`excluded.deleted_at`,
      } }).run();
    }
    for (const s of req.taskStatus) {
      tx.insert(taskStatus).values({
        participantId, taskId: s.taskId, zielanzahl: s.zielanzahl ?? null, nichtAnwendbar: s.nichtAnwendbar ? 1 : 0, updatedAt: s.updatedAt,
      }).onConflictDoUpdate({ target: [taskStatus.participantId, taskStatus.taskId],
        set: { zielanzahl: sql`excluded.zielanzahl`, nichtAnwendbar: sql`excluded.nicht_anwendbar`, updatedAt: sql`excluded.updated_at` },
        setWhere: sql`excluded.updated_at > ${taskStatus.updatedAt}`,
      }).run();
    }
  });
  return fortschritt(db, participantId);
}
```

Die übrigen Funktionen folgen `repo.ts` Zeile für Zeile (Namen der Funktionen = Namen im Interfaces-Block oben; `getDb()` wird überall durch den Parameter `db` ersetzt; `adminUpserten` entfällt). `teilnehmerAufgaben`/`aggregat` bleiben interne Helfer. `alleTeilnehmer` sortiert `ORDER BY aktiv DESC, name`; `alleTasks` `ORDER BY sort_order, nummer`.

- [ ] **Step 4: grün** · **Step 5: Mutation:** `setWhere` im TaskStatus-Upsert entfernen → LWW-Test rot; `participantId` im Execution-Insert durch `e.participantId ?? participantId` ersetzen (Typfehler ignorieren) → Fremd-Test rot. Zurücknehmen. **Step 6: Commit** `uav: Datenzugriff aus repo.ts portiert — Sync idempotent, Tombstones, LWW`

### Task 7: Die Brücke in `core/routing.ts` — `/login?code=` auf dem uav-Host

**Files:**
- Modify: `src/core/routing.ts:41-89` (`decideRoute`)
- Modify: `src/proxy.ts:31` (Aufruf: `search` mitgeben)
- Test: `src/core/routing.test.ts` (ergänzen)

**Interfaces:**
- Produces: `decideRoute({ host, pathname, groups, search? })` — `search` ist der Query-String inkl. `?` oder `""`. Neue Entscheidung: `{ action: "rewrite", target: "/m/uav/login", moduleKey: "uav" }` genau dann, wenn `moduleForHost(host)?.key === "uav"`, `pathname === "/login"` und `search` einen nicht-leeren `code`-Parameter trägt.

- [ ] **Step 1: Tests (3 Fälle + Gegenprobe, dass der Rewrite den Query behält, geschieht in proxy — dort ist `url.pathname = target` auf einem Klon von `nextUrl`, `search` bleibt)**

```ts
// in src/core/routing.test.ts ergänzen
describe("Magic-Link-Brücke uav (Spec §3 #2)", () => {
  const env = { SUITE_HOST_UAV: "uav-training.iuk-ue.de" };
  const vorher = process.env.SUITE_HOST_UAV;
  beforeAll(() => { process.env.SUITE_HOST_UAV = env.SUITE_HOST_UAV; });
  afterAll(() => { if (vorher === undefined) delete process.env.SUITE_HOST_UAV; else process.env.SUITE_HOST_UAV = vorher; });

  it("uav-Host + /login?code=… → rewrite ins Modul", () => {
    expect(decideRoute({ host: "uav-training.iuk-ue.de", pathname: "/login", groups: null, search: "?code=ABCDEFGH" }))
      .toEqual({ action: "rewrite", target: "/m/uav/login", moduleKey: "uav" });
  });
  it("uav-Host + /login ohne code → Passthrough (SSO-Login für Admins)", () => {
    expect(decideRoute({ host: "uav-training.iuk-ue.de", pathname: "/login", groups: null, search: "" })).toEqual({ action: "next" });
    expect(decideRoute({ host: "uav-training.iuk-ue.de", pathname: "/login", groups: null, search: "?code=" })).toEqual({ action: "next" });
  });
  it("fremder Host + /login?code=… → Passthrough", () => {
    expect(decideRoute({ host: "iuk-ue.de", pathname: "/login", groups: null, search: "?code=ABCDEFGH" })).toEqual({ action: "next" });
  });
});
```

- [ ] **Step 2: rot** · **Step 3: Implementierung** — in `decideRoute` **vor** dem PASSTHROUGH-Block:

```ts
export function decideRoute(input: { host: string; pathname: string; groups: string[] | null; search?: string }): RouteDecision {
  const { host, pathname, groups } = input;
  const search = input.search ?? "";

  // MAGIC-LINK-BRÜCKE uav (Spec 2026-08-28 §3 #2): verteilte Links der Form
  // https://uav-training.…/login?code=XXXX müssen ins Modul, nicht in den SSO-Login.
  // Bewusst an Host UND Parameter gebunden; ohne `code` bleibt /login der Suite-Login.
  if (pathname === "/login" && moduleForHost(host)?.key === "uav") {
    const code = new URLSearchParams(search).get("code");
    if (code && code.trim() !== "") return { action: "rewrite", target: "/m/uav/login", moduleKey: "uav" };
  }

  if (PASSTHROUGH.some(…)) …
```

In `src/proxy.ts:31`: `decideRoute({ host, pathname: nextUrl.pathname, groups, search: nextUrl.search })`.

- [ ] **Step 4: grün** — `pnpm vitest run src/core/routing.test.ts src/proxy.test.ts` · **Step 5: Commit** `core(routing): Magic-Link-Brücke — /login?code= auf dem uav-Host führt ins Modul`

---

## Phase B — HTTP-API

### Task 8: Anmeldung, Abmeldung, `me`

**Files:**
- Create: `src/app/m/uav/_lib/identitaet.ts`
- Create: `src/app/m/uav/api/anmeldung/route.ts`, `api/abmeldung/route.ts`, `api/me/route.ts`
- Test: `src/app/m/uav/api/anmeldung/route.test.ts`, `src/app/m/uav/_lib/identitaet.test.ts`

**Interfaces:**
- Produces: `identitaetAus(req: Request, db): Promise<Identity>` — liest `sid`-Cookie → Teilnehmer; sonst Suite-Session (`auth()`) → wenn `isModuleAdmin` → `{kind:'admin', id: sub, name, email}`; sonst `anon`. `POST /api/anmeldung` Body `{code}` → 200 `{ok:true}` + Set-Cookie; 401 `{error:{code:'invalid_code'}}`; 429 `{error:{code:'rate_limited'}}`. `POST /api/abmeldung` → 200, Cookie gelöscht. `GET /api/me` → `Identity`.

- [ ] **Step 1: Tests**

```ts
// src/app/m/uav/api/anmeldung/route.test.ts
import { describe, it, expect, beforeEach, vi } from "vitest";
import { rmSync } from "node:fs";
import { migrateAllModules } from "@/core/bootstrap";

const DIR = "./.data/uav-anmeldung-test";
vi.mock("@/core/auth", () => ({ auth: async () => null }));

beforeEach(async () => {
  rmSync(DIR, { recursive: true, force: true });
  process.env.DATA_DIR = DIR; process.env.SUITE_HOST_UAV = "uav-training.iuk-ue.de";
  delete (globalThis as { __suiteDb?: unknown }).__suiteDb;
  migrateAllModules();
  const { getDb } = await import("../../_db/client");
  const q = await import("../../_lib/queries");
  q.teilnehmerAnlegen(getDb(), "Ada", null);
  const p = q.alleTeilnehmer(getDb())[0];
  q.teilnehmerAendern(getDb(), p.id, {});
  process.env.__TEST_CODE = p.loginCode;
});

const post = (code: string, host = "uav-training.iuk-ue.de") =>
  new Request("http://x/api/anmeldung", { method: "POST", headers: { host, "content-type": "application/json" }, body: JSON.stringify({ code }) });

describe("POST /api/anmeldung", () => {
  it("setzt sid als httpOnly-Cookie mit path=/ und ohne Domain", async () => {
    const { POST } = await import("./route");
    const res = await POST(post(process.env.__TEST_CODE!.toLowerCase().replace(/(..)/g, "$1-")));
    expect(res.status).toBe(200);
    const sc = res.headers.get("set-cookie")!;
    expect(sc).toMatch(/^sid=/); expect(sc).toContain("Path=/"); expect(sc).toContain("HttpOnly"); expect(sc).not.toMatch(/Domain=/i);
  });
  it("falscher Code → 401 invalid_code", async () => {
    const { POST } = await import("./route");
    const res = await POST(post("ZZZZZZZZ"));
    expect(res.status).toBe(401); expect((await res.json()).error.code).toBe("invalid_code");
  });
  it("fremder Host → 404 vor jeder Prüfung", async () => {
    const { POST } = await import("./route");
    expect((await POST(post(process.env.__TEST_CODE!, "iuk-ue.de"))).status).toBe(404);
  });
  it("Rate-Limit zählt pro Code: 10 Fehlversuche in einer Minute → 429, ein anderer Code geht weiter", async () => {
    const { POST } = await import("./route");
    for (let i = 0; i < 10; i++) await POST(post("AAAAAAAA"));
    expect((await POST(post("AAAAAAAA"))).status).toBe(429);
    expect((await POST(post("BBBBBBBB"))).status).toBe(401);
  });
});
```

- [ ] **Step 2: rot** · **Step 3: Implementierung**

```ts
// src/app/m/uav/_lib/identitaet.ts
import { auth } from "@/core/auth";
import { getModule } from "@/core/registry";
import { isModuleAdmin } from "@/core/groups";
import type { UavDb } from "../_db/client";
import { SID_COOKIE, sessionValidieren, type Identity } from "./sitzung";

export function sidAusCookieHeader(header: string | null): string | null {
  if (!header) return null;
  for (const teil of header.split(";")) {
    const [k, ...v] = teil.trim().split("=");
    if (k === SID_COOKIE) return decodeURIComponent(v.join("="));
  }
  return null;
}

/** Teilnehmer-Cookie zuerst (kein Netz, kein SSO); dann Suite-Session für Admins. */
export async function identitaetAus(req: Request, db: UavDb): Promise<Identity> {
  const roh = sidAusCookieHeader(req.headers.get("cookie"));
  if (roh) { const t = sessionValidieren(db, roh); if (t) return t; }
  const session = await auth();
  const groups = session?.user?.groups ?? null;
  if (session?.user && isModuleAdmin(getModule("uav"), groups)) {
    return { kind: "admin", id: session.user.id ?? "", name: session.user.name ?? null, email: session.user.email ?? null };
  }
  return { kind: "anon" };
}
```

```ts
// src/app/m/uav/api/anmeldung/route.ts
import { NextResponse } from "next/server";
import { z } from "zod";
import { RateLimiter } from "@/core/ratelimit";
import { getDb } from "../../_db/client";
import { hostAbweisung } from "../../_lib/hostRiegel";
import { codeNormalisieren } from "../../_lib/code";
import { teilnehmerGesehen, teilnehmerPerCode } from "../../_lib/queries";
import { SID_COOKIE, sessionErzeugen, sidCookieOptionen } from "../../_lib/sitzung";

export const dynamic = "force-dynamic";
const schema = z.object({ code: z.string().min(1) });
// Pro Code, nicht pro IP (Spec §4). Prozessspeicher — Notbremse, kein Budget.
const limiter = new RateLimiter({ windowMs: 60_000, max: 10 });

const fehler = (status: number, code: string, message: string) => NextResponse.json({ error: { code, message } }, { status });

export async function POST(req: Request) {
  const abweisung = hostAbweisung(req); if (abweisung) return abweisung;
  let body: unknown; try { body = await req.json(); } catch { return fehler(400, "invalid_json", "Ungültiger JSON-Body"); }
  const parsed = schema.safeParse(body); if (!parsed.success) return fehler(400, "validation_error", "code fehlt");
  const code = codeNormalisieren(parsed.data.code);
  if (!limiter.check(code)) return fehler(429, "rate_limited", "Zu viele Versuche. Bitte später erneut versuchen.");
  const db = getDb();
  const t = code ? teilnehmerPerCode(db, code) : null;
  if (!t) return fehler(401, "invalid_code", "Ungültiger oder inaktiver Code.");
  teilnehmerGesehen(db, t.id);
  const res = NextResponse.json({ ok: true });
  res.cookies.set(SID_COOKIE, sessionErzeugen(db, t.id), sidCookieOptionen());
  return res;
}
```

`api/abmeldung/route.ts`: Riegel → `sessionLoeschen(db, sidAusCookieHeader(...))` → `res.cookies.set(SID_COOKIE, "", { ...sidCookieOptionen(), maxAge: 0 })` → `{ok:true}`.
`api/me/route.ts`: Riegel → `Response.json(await identitaetAus(req, getDb()))`.

- [ ] **Step 4: grün** · **Step 5: Commit** `uav: Anmeldung per Code, Abmeldung, me — Alt-Cookie-Vertrag`

### Task 9: `tasks`, `progress`, `sync`

**Files:**
- Create: `src/app/m/uav/api/tasks/route.ts`, `api/progress/route.ts`, `api/sync/route.ts`
- Create: `src/app/m/uav/_lib/syncSchema.ts` (Zod aus `uav-praxis/server/routes/sync.ts:19-36` kopiert)
- Test: `src/app/m/uav/api/sync/route.test.ts`

**Interfaces:**
- `GET /api/tasks` → Teilnehmer **oder** Admin, sonst 401 · `GET /api/progress` → nur Teilnehmer · `POST /api/sync` Body `SyncRequest` → `ProgressSnapshot`; `participantId` aus der Identität.

- [ ] **Step 1: Test** — anonym → 401; mit Cookie aus Task 8 → 200 und Snapshot; Body ohne `executions` → 400; fremder Host → 404. (Aufbau wie `anmeldung/route.test.ts`: erst `POST anmeldung`, `set-cookie` in den nächsten Request als `cookie` übernehmen.)

- [ ] **Step 2–4:** rot → Implementierung (jeder Handler: `hostAbweisung` → `identitaetAus` → Gate → `queries`) → grün.
- [ ] **Step 5: Commit** `uav: Katalog, Fortschritt und Sync als Route Handler`

### Task 10: Admin-API, CSV, Admin-Guard

**Files:**
- Create: `src/app/m/uav/_lib/requireUavAdmin.ts`, `_lib/csv.ts`, `_lib/adminSchemas.ts` (Zod aus `admin.ts` kopiert)
- Create: die sieben Handler unter `api/admin/…` (Dateistruktur oben)
- Test: `src/app/m/uav/_lib/csv.test.ts`, `src/app/m/uav/api/admin/participants/route.test.ts`

**Interfaces:**
- `requireUavAdminPage(): Promise<void>` (Seiten → `notFound()`), `requireUavAdminAction(): Promise<void>` (Server Actions → throw), `adminAbweisung(req): Promise<Response|null>` (Handler → 403 JSON; Reihenfolge: erst `hostAbweisung`, dann dieser).
- `csvAntwort(zeilen: string[][], dateiname): Response` — BOM, CRLF, jedes Feld in `"…"`, Formel-Neutralisierung `^[\t\r]*[=+\-@]` → `'`-Präfix (Alt `admin.ts:12-15`).

- [ ] **Step 1: Tests**

```ts
// src/app/m/uav/_lib/csv.test.ts
import { it, expect } from "vitest";
import { csvFeld, csvAntwort } from "./csv";
it("neutralisiert Formeln und maskiert Anführungszeichen", () => {
  expect(csvFeld("=SUM(A1)")).toBe(`"'=SUM(A1)"`);
  expect(csvFeld("\t+1")).toBe(`"'\t+1"`);
  expect(csvFeld('Say "hi"')).toBe(`"Say ""hi"""`);
  expect(csvFeld("Ada")).toBe(`"Ada"`);
});
it("liefert BOM, CRLF, text/csv und attachment", async () => {
  const r = csvAntwort([["a", "b"], ["1", "2"]], "x.csv");
  expect(r.headers.get("content-type")).toContain("text/csv");
  expect(r.headers.get("content-disposition")).toBe(`attachment; filename="x.csv"`);
  expect(await r.text()).toBe('﻿"a","b"\r\n"1","2"\r\n');
});
```

```ts
// src/app/m/uav/api/admin/participants/route.test.ts — Gating
import { describe, it, expect, vi, beforeEach } from "vitest";
let gruppen: string[] | null = null;
vi.mock("@/core/auth", () => ({ auth: async () => (gruppen ? { user: { id: "sub-1", name: "Root", email: "r@x", groups: gruppen } } : null) }));
// DATA_DIR/migrate wie in Task 8
const get = (host = "uav-training.iuk-ue.de") => new Request("http://x/api/admin/participants", { headers: { host } });
describe("GET /api/admin/participants", () => {
  it("anonym → 403 (nicht 401, nicht 404 — der Host ist richtig, das Recht fehlt)", async () => { gruppen = null; const { GET } = await import("./route"); expect((await GET(get())).status).toBe(403); });
  it("eingeloggt ohne Gruppe → 403", async () => { gruppen = ["andere"]; const { GET } = await import("./route"); expect((await GET(get())).status).toBe(403); });
  it("mit uav-training-admin → 200 und Liste", async () => { gruppen = ["uav-training-admin"]; const { GET } = await import("./route"); const r = await GET(get()); expect(r.status).toBe(200); expect(await r.json()).toEqual([]); });
  it("Suite-Admin (dashboard-admins) → 200", async () => { gruppen = ["dashboard-admins"]; const { GET } = await import("./route"); expect((await GET(get())).status).toBe(200); });
  it("fremder Host → 404 auch für Admins", async () => { gruppen = ["uav-training-admin"]; const { GET } = await import("./route"); expect((await GET(get("iuk-ue.de"))).status).toBe(404); });
});
```

- [ ] **Step 2: rot** · **Step 3: Implementierung**

```ts
// src/app/m/uav/_lib/requireUavAdmin.ts
import { notFound } from "next/navigation";
import { auth } from "@/core/auth";
import { getModule } from "@/core/registry";
import { isModuleAdmin } from "@/core/groups";

async function darf(): Promise<boolean> { const s = await auth(); return isModuleAdmin(getModule("uav"), s?.user?.groups ?? null); }
export async function requireUavAdminPage(): Promise<void> { if (!(await darf())) notFound(); }
export async function requireUavAdminAction(): Promise<void> { if (!(await darf())) throw new Error("Forbidden"); }
/** Für Route Handler: NACH hostAbweisung rufen. */
export async function adminAbweisung(): Promise<Response | null> {
  return (await darf()) ? null : Response.json({ error: { code: "forbidden", message: "Verwaltung nur für Admins" } }, { status: 403 });
}
```

Handler-Muster (alle sieben identisch aufgebaut):

```ts
// src/app/m/uav/api/admin/participants/route.ts
import { NextResponse } from "next/server";
import { getDb } from "../../../_db/client";
import { hostAbweisung } from "../../../_lib/hostRiegel";
import { adminAbweisung } from "../../../_lib/requireUavAdmin";
import { teilnehmerAnlegenSchema } from "../../../_lib/adminSchemas";
import { teilnehmerAnlegen, teilnehmerUebersicht } from "../../../_lib/queries";
export const dynamic = "force-dynamic";
export async function GET(req: Request) {
  return hostAbweisung(req) ?? (await adminAbweisung()) ?? Response.json(teilnehmerUebersicht(getDb()));
}
export async function POST(req: Request) {
  const ab = hostAbweisung(req) ?? (await adminAbweisung()); if (ab) return ab;
  const parsed = teilnehmerAnlegenSchema.safeParse(await req.json().catch(() => null));
  if (!parsed.success) return NextResponse.json({ error: { code: "validation_error", message: parsed.error.message } }, { status: 400 });
  return NextResponse.json(teilnehmerAnlegen(getDb(), parsed.data.name, parsed.data.beginn ?? null), { status: 201 });
}
```

Die übrigen: `participants/export` (Spalten `Name, Beginn, Erledigt, Gesamt, Quote, LetzteAktivität, Status` — Alt `admin.ts:53-64`), `participants/[id]` (GET Detail, PATCH, DELETE), `participants/[id]/export` (Spalten `Teil, Nummer, Titel, Anzahl, Ziel, Erledigt, NichtAnwendbar, LetzteDurchführung`, Dateiname `teilnehmer-<slug>-auswertung.csv`), `tasks` (GET inkl. inaktiv, POST), `tasks/[id]` (PATCH, DELETE), `tasks/reorder` (POST `{ids}`). `NotFound` → 404 JSON.

- [ ] **Step 4: grün** · **Step 5: Mutation:** in `adminAbweisung` `darf()` durch `true` ersetzen → drei Tests rot. **Step 6: Commit** `uav: Admin-API mit Gruppen-Gate und formelsicherer CSV`

---

## Phase C — Teilnehmer-Oberfläche (öffentliche Klasse: eigene CSS-Module, kein antd)

### Task 11: Offline-Speicher und Sync-Engine portieren (Keys unverändert)

**Files:**
- Create: `src/app/m/uav/_ui/offline/localStore.ts`, `syncEngine.ts`, `client.ts`, `progress.ts`
- Test: `src/app/m/uav/_ui/offline/localStore.test.ts`, `syncEngine.test.ts` (jsdom)

**Interfaces:**
- `localStore` mit exakt den Methoden aus `uav-praxis/src/offline/localStore.ts` (`katalog`, `fortschrittStateLesen/-Schreiben/-Abonnieren`, `queueLesen/-Anfuegen/-AlsSyncMutationen/-BestaetigteEntfernen`, `lastSyncLesen/-Schreiben`, `uebernommenGesetzt/-Markieren`, `snapshotAnwenden`). Unterschied: `fallbackKatalog()` liefert `[]` — der Katalog kommt vom Server (`/api/tasks`) und liegt danach im Cache; der 17,9-KB-Seed wird nicht mitportiert (Spec §2).
- `syncEngine` (Singleton) mit `start()`, `stop()`, `mutationGemeldet()`, `triggerSync()`, `abonnieren(cb)`, `statusLesen()`.
- `api` (client.ts): `BASIS = "/api"`; Methoden `me`, `participantLogin(code)` → `POST /anmeldung`, `logout` → `POST /abmeldung`, `getTasks`, `getProgress`, `sync`; **keine** Admin-Methoden (die Verwaltung ist RSC).

- [ ] **Step 1: Tests**

```ts
// src/app/m/uav/_ui/offline/localStore.test.ts
/** @vitest-environment jsdom */
import { describe, it, expect, beforeEach } from "vitest";
import { localStore } from "./localStore";

beforeEach(() => localStorage.clear());

describe("localStore — die Alt-Keys tragen den Umzug", () => {
  it("liest eine Queue, die die Alt-Anwendung unter drk-drohnen-sync-queue hinterlassen hat", () => {
    localStorage.setItem("drk-drohnen-sync-queue", JSON.stringify([
      { art: "execution", daten: { id: "alt-1", taskId: "1-1", datum: "2026-08-20", drohnensteuerer: "A", luftraumbeobachter: "B" } },
      { art: "taskStatus", daten: { taskId: "1-2", zielanzahl: 3, nichtAnwendbar: false, updatedAt: "2026-08-20T10:00:00.000Z" } },
    ]));
    const m = localStore.queueAlsSyncMutationen();
    expect(m.executions.map((e) => e.id)).toEqual(["alt-1"]);
    expect(m.taskStatus[0].zielanzahl).toBe(3);
  });
  it("schreibt unter den Alt-Keys, nicht unter neuen", () => {
    localStore.lastSyncSchreiben("2026-08-28T00:00:00.000Z");
    localStore.tasksSchreiben([]);
    expect(Object.keys(localStorage).sort()).toEqual(["drk-drohnen-katalog", "drk-drohnen-last-sync"]);
  });
  it("queueAnfuegen ersetzt eine Execution gleicher id und hält beim TaskStatus den jüngeren Stand", () => {
    localStore.queueAnfuegen({ art: "execution", daten: { id: "x", taskId: "1-1", datum: "2026-08-01", drohnensteuerer: "", luftraumbeobachter: "" } });
    localStore.queueAnfuegen({ art: "execution", daten: { id: "x", taskId: "1-1", datum: "2026-08-02", drohnensteuerer: "", luftraumbeobachter: "" } });
    localStore.queueAnfuegen({ art: "taskStatus", daten: { taskId: "1-1", zielanzahl: 2, nichtAnwendbar: false, updatedAt: "2026-08-02T00:00:00.000Z" } });
    localStore.queueAnfuegen({ art: "taskStatus", daten: { taskId: "1-1", zielanzahl: 9, nichtAnwendbar: false, updatedAt: "2026-08-01T00:00:00.000Z" } });
    const q = localStore.queueLesen();
    expect(q).toHaveLength(2);
    expect(q.find((e) => e.art === "execution")?.daten).toMatchObject({ datum: "2026-08-02" });
    expect(q.find((e) => e.art === "taskStatus")?.daten).toMatchObject({ zielanzahl: 2 });
  });
  it("snapshotAnwenden: Tombstones löschen, pending gewinnt, Ziel mindestens 1", () => {
    localStore.tasksSchreiben([{ id: "1-1", teil: 1, nummer: "1.1", titel: "t", lernziel: "", schritte: [], durchfuehrungshinweise: [], sicherheitshinweise: [], zielanzahlDefault: 2, sortOrder: 0, aktiv: true }]);
    const state = localStore.snapshotAnwenden(
      { executions: [{ id: "a", taskId: "1-1", datum: "2026-08-01", drohnensteuerer: "", luftraumbeobachter: "" }, { id: "b", taskId: "1-1", datum: "2026-08-02", drohnensteuerer: "", luftraumbeobachter: "", deletedAt: "2026-08-03T00:00:00.000Z" }], taskStatus: [], serverTime: "2026-08-28T00:00:00.000Z" },
      { executions: [], taskStatus: [{ taskId: "1-1", zielanzahl: 0, nichtAnwendbar: false, updatedAt: "x" }] },
    );
    expect(state.fortschritt["1-1"].durchfuehrungen.map((d) => d.id)).toEqual(["a"]);
    expect(state.fortschritt["1-1"].zielanzahl).toBe(1);
  });
});
```

```ts
// src/app/m/uav/_ui/offline/syncEngine.test.ts
/** @vitest-environment jsdom */
import { describe, it, expect, beforeEach, vi } from "vitest";
import { localStore } from "./localStore";
import { syncEngine } from "./syncEngine";
import { api, ApiError } from "./client";

beforeEach(() => { localStorage.clear(); vi.restoreAllMocks(); });

describe("syncEngine", () => {
  it("schickt die Queue, entfernt nur Bestätigtes, schreibt lastSync und wendet den Snapshot an", async () => {
    localStore.queueAnfuegen({ art: "execution", daten: { id: "q1", taskId: "1-1", datum: "2026-08-01", drohnensteuerer: "", luftraumbeobachter: "" } });
    const spy = vi.spyOn(api, "sync").mockResolvedValue({ executions: [], taskStatus: [], serverTime: "2026-08-28T12:00:00.000Z" });
    await syncEngine.syncJetzt();
    expect(spy.mock.calls[0][0].executions.map((e) => e.id)).toEqual(["q1"]);
    expect(localStore.queueLesen()).toEqual([]);
    expect(localStore.lastSyncLesen()).toBe("2026-08-28T12:00:00.000Z");
    expect(syncEngine.statusLesen()).toBe("synced");
  });
  it("Netzfehler (status 0) → offline, Queue bleibt", async () => {
    localStore.queueAnfuegen({ art: "execution", daten: { id: "q1", taskId: "1-1", datum: "2026-08-01", drohnensteuerer: "", luftraumbeobachter: "" } });
    vi.spyOn(api, "sync").mockRejectedValue(new ApiError(0, "network_error", "x"));
    await syncEngine.syncJetzt();
    expect(localStore.queueLesen()).toHaveLength(1);
    expect(syncEngine.statusLesen()).toBe("offline");
  });
  it("401 → fehler (nicht offline), Queue bleibt", async () => {
    vi.spyOn(api, "sync").mockRejectedValue(new ApiError(401, "unauthorized", "x"));
    await syncEngine.syncJetzt();
    expect(syncEngine.statusLesen()).toBe("fehler");
  });
});
```

- [ ] **Step 2: rot** · **Step 3: Portieren** — `localStore.ts`, `syncEngine.ts`, `client.ts` aus `uav-praxis/src/offline/` bzw. `src/api/client.ts` kopieren; `progress.ts` aus `src/domain/progress.ts`. Änderungen: Imports auf `../../_lib/typen`; `AUFGABEN`-Import und `initialerFortschritt()` durch leere Map ersetzen (`fallbackKatalog()` → `[]`); `client.ts`: `participantLogin` → `/anmeldung`, `logout` → `/abmeldung`, Admin-Methoden löschen. Alle Dateien ohne `"use client"` (reine Module; die Komponenten sind die Client-Grenze).
- [ ] **Step 4: grün** · **Step 5: Commit** `uav: Offline-Speicher, Sync-Engine und API-Client portiert — Storage-Keys bleiben`

### Task 12: Teilnehmer-Insel — Dashboard, Aufgabe, Erfassung

**Files:**
- Create: `src/app/m/uav/_ui/teilnehmer/{TeilnehmerApp,Dashboard,TaskCard,TaskDetail,DurchfuehrungForm,AnzahlFeld,SyncStatus}.tsx`, `useFortschritt.ts`, `useKatalog.ts`, `uav.module.css`
- Create: `src/app/m/uav/layout.tsx`, `page.tsx`, `aufgabe/page.tsx`, `aufgabe/[id]/route.ts`
- Test: `src/app/m/uav/_ui/teilnehmer/DurchfuehrungForm.test.tsx`, `Dashboard.test.tsx`, `AnzahlFeld.test.tsx` (aus den Alt-Tests portiert), `src/app/m/uav/aufgabe/[id]/route.test.ts`

**Interfaces:**
- `<TeilnehmerApp ansicht="start" | "aufgabe" />` — `"use client"`; liest `?id=` mit `useSearchParams()`; bei `identity.kind !== "participant"` (via `api.me()`, gecacht in `sessionStorage`) rendert sie den Hinweis „Bitte mit deinem Code anmelden" mit Link `/login` — **nicht** redirect (offline muss die Insel stehen).
- `aufgabe/[id]/route.ts`: `GET` → 308 auf `/aufgabe?id=<id>` (äußere Form).

- [ ] **Step 1: Tests** — Alt-Tests `uav-praxis/src/components/{DurchfuehrungForm,Dashboard,AnzahlFeld}.test.tsx` übernehmen (Imports anpassen, `@vitest-environment jsdom`). Dazu:

```ts
// src/app/m/uav/aufgabe/[id]/route.test.ts
import { it, expect } from "vitest";
import { GET } from "./route";
it("Alt-Bookmark /aufgabe/1-1 → 308 /aufgabe?id=1-1", async () => {
  const r = await GET(new Request("http://uav-training.iuk-ue.de/aufgabe/1-1", { headers: { host: "uav-training.iuk-ue.de" } }), { params: Promise.resolve({ id: "1-1" }) });
  expect(r.status).toBe(308); expect(r.headers.get("location")).toBe("/aufgabe?id=1-1");
});
```

- [ ] **Step 2: rot** · **Step 3: Portieren**
  - Komponenten aus `uav-praxis/src/components/` und `src/pages/{StartPage,TeilnehmerApp}.tsx`, Hooks aus `src/hooks/`. TanStack-Router-Links → `next/link` mit `href="/aufgabe?id=…"`; `useSearch` → `useSearchParams`. Bilder: `bildUrl` aus dem Katalog **ohne** Umschreibung (Task 21 setzt im Import `/m/uav/illustrations/<id>.webp`; auf dem uav-Host ist das über den Rewrite erreichbar? **Nein** — `/_next`, `/favicon.ico` sind Passthrough, `public/`-Assets unter `/m/uav/…` werden auf dem uav-Host als `/m/uav/m/uav/…` gesucht. Deshalb: Bildpfad in der DB = `/m/uav/illustrations/<id>.webp`, und die Insel rendert `<img src={bildUrl}>` — da `/m/uav/illustrations/x.webp` in `decideRoute` als interner Pfad erkannt wird (`^\/m\/`), geht er mit `next` durch und trifft `public/`. Test in Task 21.)
  - `styles.css` → `uav.module.css` (Klassen 1:1, Selektoren auf Module-Klassen). Tap-Höhen ≥ 56 px für Erfassen-Buttons (Design-System, Handschuh).
  - `layout.tsx`:

```tsx
// src/app/m/uav/layout.tsx
import type { Metadata } from "next";
import { Shell } from "@/core/shell/Shell";
import { getModule } from "@/core/registry";
import { canAdminModule } from "@/core/auth/guards";
import { RegisterSW } from "./RegisterSW";
export const metadata: Metadata = { manifest: "/manifest.webmanifest" };
export default async function UavLayout({ children }: { children: React.ReactNode }) {
  const mod = getModule("uav");
  const darfVerwalten = await canAdminModule("uav");
  return (
    <Shell variant={mod.shell} moduleKey={mod.key}
      nav={[{ key: "start", title: "Training", href: "/" }, ...(darfVerwalten ? [{ key: "admin", title: "Verwaltung", href: "/admin" }] : [])]}>
      <RegisterSW />
      {children}
    </Shell>
  );
}
```

  `page.tsx`: `export default function Page() { return <TeilnehmerApp ansicht="start" />; }` · `aufgabe/page.tsx`: `<Suspense><TeilnehmerApp ansicht="aufgabe" /></Suspense>` (`useSearchParams` braucht Suspense).

- [ ] **Step 4: grün** — `pnpm vitest run src/app/m/uav` · **Step 5: Commit** `uav: Teilnehmer-Insel portiert — Dashboard, Aufgabe, Erfassung, offline-fähig`

### Task 13: Login-Seite und Magic-Link-Einlösung

**Files:**
- Create: `src/app/m/uav/login/page.tsx`, `_ui/teilnehmer/LoginForm.tsx`
- Test: `src/app/m/uav/_ui/teilnehmer/LoginForm.test.tsx` (aus `uav-praxis/src/pages/LoginPage.test.tsx`)

**Interfaces:**
- `<LoginForm code?: string />` — bei gesetztem `code`: sofort `api.participantLogin(code)`, Erfolg → `window.location.replace("/")` (voller Reload, damit die Insel mit Session startet); Fehler → Feld vorbefüllt + Meldung. Ohne `code`: Eingabefeld (autoCapitalize off, inputMode text, 56 px), Button „Anmelden".
- Seite: `export default function Login({ searchParams }) { const { code } = await searchParams; return <LoginForm code={typeof code === "string" ? code : undefined} />; }` — plus Link „Verwaltung (Anmeldung mit Suite-Konto)" → `/login?callbackUrl=/admin` **auf dem Apex** (`https://iuk-ue.de/login…`)? Nein: einfach `/api/auth/signin?callbackUrl=%2Fadmin` (Passthrough, Auth.js-Signin).

- [ ] **Step 1–5:** Test (Auto-Einlösung ruft `api.participantLogin` genau einmal mit dem Rohwert; Fehler 401 zeigt „Ungültiger oder inaktiver Code."; 429 zeigt die Server-Meldung) → rot → Implementierung → grün → Commit `uav: Login-Seite, Magic-Link löst den Code sofort ein`

### Task 14: Manifest, Icon, Service Worker (zwei Modi) und Boot-Prüfung

**Files:**
- Create: `src/app/m/uav/_lib/sw-quelle.ts`, `_lib/boot.ts`, `sw.js/route.ts`, `manifest.webmanifest/route.ts`, `pwa-icon.svg/route.ts`, `RegisterSW.tsx`
- Modify: `src/core/bootstrap.ts:92-109` (`...(await uavBootFehler())`)
- Test: `src/app/m/uav/_lib/sw-quelle.test.ts`, `_lib/boot.test.ts`, `sw.js/route.test.ts`

**Interfaces:**
- `UAV_SW_ABRAEUM_QUELLE: string` (kein `fetch`-Handler; `caches.keys()` löschen; `unregister()`; `clients.claim()`; danach `clients.matchAll()` → `client.navigate(client.url)`, damit die Alt-Oberfläche sofort durch die Suite ersetzt wird).
- `UAV_SW_CACHE_QUELLE: string` — nach `qr/_lib/sw-source.ts`: `CACHE = "uav-pwa-v1"`, `SHELL_ROUTES = ["/", "/aufgabe", "/login"]`, `credentials: "omit"` **nicht** — hier ist die Shell-Seite in beiden Session-Zuständen dieselbe Insel ohne Daten im HTML (die Daten kommen aus `localStorage`/API), also darf mit Cookies geholt werden; **Allowlist:** `/_next/static/`, `/m/uav/illustrations/`, die drei Shell-Routen; **nie** `/api/`, nie `/admin`, nie `/m/uav/admin`.
- `swModus(env): "abraeumen" | "cachen"`; `uavBootFehler(): Promise<string[]>` — wenn `SUITE_HOST_UAV` gesetzt und `UAV_SW_MODUS` fehlt oder fremd → Fehlertext; wirft nie.
- `RegisterSW`: registriert `/sw.js` mit `scope: "/"` **nur** wenn der Server im Modus `cachen` ist — die Seite bekommt das als Prop aus dem Layout (`swModus(process.env)`). Im Modus `abraeumen` registriert nichts (der Alt-Worker holt `/sw.js` bei seiner Update-Prüfung selbst).

- [ ] **Step 1: Tests**

```ts
// src/app/m/uav/_lib/sw-quelle.test.ts
import { describe, it, expect } from "vitest";
import { UAV_SW_ABRAEUM_QUELLE, UAV_SW_CACHE_QUELLE } from "./sw-quelle";
describe("Abräum-Worker", () => {
  it("hat keinen fetch-Handler, räumt alle Caches, trägt sich aus und lädt Clients neu", () => {
    expect(UAV_SW_ABRAEUM_QUELLE).not.toContain('addEventListener("fetch"');
    expect(UAV_SW_ABRAEUM_QUELLE).toContain("caches.keys()");
    expect(UAV_SW_ABRAEUM_QUELLE).toContain("registration.unregister()");
    expect(UAV_SW_ABRAEUM_QUELLE).toContain("client.navigate(");
  });
});
describe("Cache-Worker", () => {
  it("cacht nie /api/ oder die Verwaltung", () => {
    expect(UAV_SW_CACHE_QUELLE).not.toMatch(/SHELL_ROUTES = \[[^\]]*api/);
    expect(UAV_SW_CACHE_QUELLE).toContain('if (url.pathname.startsWith("/api/")) return;');
    expect(UAV_SW_CACHE_QUELLE).toContain('if (url.pathname.startsWith("/admin")) return;');
  });
  it("gibt nicht weitergereichte Antworten frei (Lehre aus qr)", () => {
    expect(UAV_SW_CACHE_QUELLE).toContain("function releaseBody(");
  });
});
```

```ts
// src/app/m/uav/_lib/boot.test.ts
import { it, expect } from "vitest";
import { uavBootFehler, swModus } from "./boot";
it("ohne SUITE_HOST_UAV: nichts zu prüfen", async () => expect(await uavBootFehler({})).toEqual([]));
it("mit Host, ohne Modus → Fehlertext, kein Wurf", async () => {
  const f = await uavBootFehler({ SUITE_HOST_UAV: "uav-training.iuk-ue.de" });
  expect(f).toHaveLength(1); expect(f[0]).toContain("UAV_SW_MODUS");
});
it("fremder Wert → Fehler; gültige Werte → keiner", async () => {
  expect(await uavBootFehler({ SUITE_HOST_UAV: "x", UAV_SW_MODUS: "aus" })).toHaveLength(1);
  expect(await uavBootFehler({ SUITE_HOST_UAV: "x", UAV_SW_MODUS: "abraeumen" })).toEqual([]);
  expect(swModus({ UAV_SW_MODUS: "cachen" })).toBe("cachen");
  expect(swModus({})).toBe("abraeumen");   // Vorgabe: die sichere Seite
});
```

`sw.js/route.test.ts`: fremder Host → 404; eigener Host + `UAV_SW_MODUS=abraeumen` → Body enthält `registration.unregister()`; `=cachen` → Body enthält `uav-pwa-v1`; `content-type: text/javascript`, `cache-control: no-cache`.

- [ ] **Step 2: rot** · **Step 3: Implementierung** — Abräum-Quelle aus `radio/_lib/sw-quelle.ts:555-573` übernehmen und um den Reload ergänzen:

```js
      await self.clients.claim();
      await self.registration.unregister();
      const clients = await self.clients.matchAll({ type: "window" });
      for (const client of clients) { try { await client.navigate(client.url); } catch {} }
```

Cache-Quelle: `qr/_lib/sw-source.ts` als Vorlage, mit `SHELL_ROUTES = ["/", "/aufgabe", "/login"]`, `fetch(path)` **mit** Cookies (Begründung oben als Kommentar in der Datei), im `fetch`-Handler zuerst die zwei `return`-Zeilen aus dem Test. `boot.ts`:

```ts
export type SwModus = "abraeumen" | "cachen";
type EnvLike = Record<string, string | undefined>;
export function swModus(env: EnvLike = process.env): SwModus { return env.UAV_SW_MODUS === "cachen" ? "cachen" : "abraeumen"; }
export async function uavBootFehler(env: EnvLike = process.env): Promise<string[]> {
  if (!env.SUITE_HOST_UAV) return [];
  const m = env.UAV_SW_MODUS;
  if (m !== "abraeumen" && m !== "cachen") return [`UAV_SW_MODUS muss bei gesetztem SUITE_HOST_UAV "abraeumen" oder "cachen" sein (ist: ${JSON.stringify(m)}). Spec 2026-08-28 §5.`];
  return [];
}
```

Manifest wie `qr/manifest.webmanifest/route.ts` mit `name: "Drohnen-Trainingsbegleiter"`, `short_name: "Drohnen-Training"`, `theme_color: "#c8000f"` (Suite-Rot statt Alt-`#e30613`), `start_url: "/"`, `scope: "/"`. Icon: `pwa-icon.svg` aus `qr` kopieren, Motiv Drohne (einfaches Inline-SVG genügt).

- [ ] **Step 4: grün** · **Step 5: Commit** `uav: Manifest, Abräum- und Cache-Worker, Boot-Prüfung UAV_SW_MODUS`

---

## Phase D — Verwaltung (antd, Voll-Shell)

### Task 15: Admin-Layout und Teilnehmerliste

**Files:**
- Create: `src/app/m/uav/(admin)/layout.tsx`, `(admin)/admin/page.tsx`, `_ui/admin/TeilnehmerTabelle.tsx`, `_ui/admin/TeilnehmerAnlegen.tsx`, `_actions/teilnehmer.ts`
- Test: `src/app/m/uav/_actions/teilnehmer.test.ts`, `src/app/m/uav/(admin)/admin/page.test.tsx`

**Interfaces:**
- Actions (alle `"use server"`, erste Zeile `await requireUavAdminAction()`): `teilnehmerAnlegenAction(fd: FormData)` (name, beginn) · `teilnehmerAendernAction(id, patch)` · `teilnehmerLoeschenAction(id)` · `codeNeuAction(id)`; nach jeder Änderung `revalidatePath("/m/uav/admin")`.
- Seite: `requireUavAdminPage()` → `teilnehmerUebersicht(getDb())` → `<TeilnehmerTabelle zeilen=…/>` (antd `Table`, Spalten Name · Beginn · Fortschritt (`Progress`-Balken ohne Rot) · letzte Aktivität · Status · Aktionen) + Button „CSV" → `<a href="/api/admin/participants/export">` + `TeilnehmerAnlegen` (Formular: Name, Beginn).

- [ ] **Step 1: Tests** — Actions: ohne Gruppe wirft `Forbidden` **bevor** etwas geschrieben wurde (DB leer danach); mit Gruppe legt an und der Code hat 8 Zeichen. Seite: `page.test.tsx` rendert mit gemocktem `auth` (Gruppe) und findet den Namen; ohne Gruppe `notFound` (Mock von `next/navigation`).
- [ ] **Step 2–5:** rot → Implementierung (Layout wie `feedback/(admin)/layout.tsx`, mit `requireUavAdminPage()` statt `requireFeedbackAccess()`, Nav: Teilnehmer `/admin` · Katalog `/admin/katalog` · Training `/`) → grün → Commit `uav: Verwaltung — Layout und Teilnehmerliste`

### Task 16: Teilnehmer-Detail mit Code, Magic-Link und Export

**Files:**
- Create: `src/app/m/uav/(admin)/admin/teilnehmer/[id]/page.tsx`, `_ui/admin/TeilnehmerDetail.tsx`, `_lib/magicLink.ts`
- Test: `src/app/m/uav/_lib/magicLink.test.ts`

**Interfaces:**
- `magicLink(code: string, env = process.env): string` — Host = `prodHostsFor(getModule("uav"), env)[0]`; ohne Prod-Host (Dev) `uav.localtest.me:3000`; Ergebnis `https://<host>/login?code=<code>` (Dev `http://`). **Nicht** `AUTH_URL`, nicht `headers().host` (Lehre feedback-QR-Befund).
- Detail zeigt: Name/Beginn/Status editierbar, Code groß (`SCHRIFT.zahl`), Magic-Link mit „Kopieren", Button „Neuen Code erzeugen" (Bestätigung: alte Links werden ungültig), Fortschritt je Teil, Aufgabenliste, CSV-Link `/api/admin/participants/<id>/export`, Löschen mit Bestätigung.

- [ ] **Step 1: Test**

```ts
import { it, expect } from "vitest";
import { magicLink } from "./magicLink";
it("baut den Link aus SUITE_HOST_UAV, nie aus AUTH_URL", () => {
  expect(magicLink("ABCDEFGH", { SUITE_HOST_UAV: "uav-training.iuk-ue.de", AUTH_URL: "https://iuk-ue.de" })).toBe("https://uav-training.iuk-ue.de/login?code=ABCDEFGH");
  expect(magicLink("ABCDEFGH", {})).toBe("http://uav.localtest.me:3000/login?code=ABCDEFGH");
});
```

- [ ] **Step 2–5:** rot → Implementierung → grün → Commit `uav: Teilnehmer-Detail mit Magic-Link aus dem Prod-Host`

### Task 17: Katalog-Verwaltung

**Files:**
- Create: `src/app/m/uav/(admin)/admin/katalog/page.tsx`, `_ui/admin/KatalogTabelle.tsx`, `_ui/admin/AufgabeFormular.tsx`, `_actions/katalog.ts`
- Test: `src/app/m/uav/_actions/katalog.test.ts`

**Interfaces:**
- Actions: `aufgabeAnlegenAction(eingabe)`, `aufgabeAendernAction(id, patch)`, `aufgabeLoeschenAction(id)`, `aufgabenSortierenAction(ids)` — Validierung mit den Zod-Schemas aus `_lib/adminSchemas.ts` (Task 10), `bildUrl` getrimmt/`null`.
- Tabelle: Teil · Nummer · Titel · Ziel · aktiv · Bild-Vorschau · Hoch/Runter (Sortierung per Buttons, kein Drag — Tap-tauglich) · Bearbeiten (Drawer mit Formular: Listenfelder als Textarea „eine Zeile je Eintrag").

- [ ] **Step 1–5:** Tests (Reorder schreibt Positionen; Löschen unbekannt → wirft `NotFound`; ohne Gruppe `Forbidden`) → rot → Implementierung → grün → Commit `uav: Katalog-Verwaltung`

---

## Phase E — Import, Seed, Illustrationen, E2E, Betrieb

### Task 18: Import-Skript SQLite → SQLite mit Paritätscheck

**Files:**
- Create: `scripts/import/uav.ts`, `scripts/import/uav.test.ts`
- Modify: `package.json` scripts: `"import:uav": "tsx scripts/import/uav.ts"`

**Interfaces:**
- CLI: `pnpm import:uav <pfad-zur-alt-app.db>` (Snapshot aus `sqlite3 app.db ".backup snap.db"`). `DATA_DIR` bestimmt das Ziel.
- `importUav(quelle: Database.Database, ziel: UavDb, jetzt = new Date()): { participants; tasks; executions; taskStatus; sessions; sessionsUebersprungen }` — Upsert per PK, alle Spalten; `sessions`: nur `kind='participant'` **und** `expires_at > jetzt`; `tasks.bild`: `/illustrations/<x>` → `/m/uav/illustrations/<x>` (nur wenn es mit `/illustrations/` beginnt).
- `paritaetUav(quelle, ziel): ParityReport[]` — je Tabelle `checkParity` über volle Zeilen (bei `tasks` mit dem **Dateinamen** von `bild` statt dem Pfad; bei `sessions` nur die importierbaren Zeilen der Quelle) plus Prüfung `Set(login_code)` gleich.

- [ ] **Step 1: Tests**

```ts
// scripts/import/uav.test.ts (Auszug — die Fälle sind Pflicht)
import Database from "better-sqlite3";
import { readFileSync } from "node:fs";
// Quelle: In-Memory-DB mit dem Alt-Schema — WÖRTLICH aus uav-praxis/server/db/schema.sql
// (die Datei nach scripts/import/fixtures/uav-alt-schema.sql kopieren).
function altDb() { const db = new Database(":memory:"); db.exec(readFileSync("scripts/import/fixtures/uav-alt-schema.sql", "utf8")); return db; }

it("kopiert alle fünf Tabellen ID-erhaltend, Paritätscheck grün", …);
it("ist idempotent — und ein zweiter Lauf nach einer Änderung in der Quelle ÄNDERT die Zielzeile", …);   // onConflictDoNothing → rot
it("überspringt abgelaufene und admin-Sessions, zählt sie", …);
it("schreibt das Bildpräfix um; die Parität vergleicht Dateinamen", …);
it("Parität schlägt an, wenn im Ziel ein login_code fehlt", …);
it("bricht ab (assertParity wirft), bevor es ‚fertig' meldet", …);
```

- [ ] **Step 2–5:** rot → Implementierung (Muster `scripts/import/qr.ts`/`portal.ts`: `migrateAllModules()` zuerst, `getModuleDb("uav", schema)`, Upserts, `assertParity`) → grün → Commit `uav: Import aus der Alt-SQLite mit Paritätscheck und Session-Übernahme`

### Task 19: Lokaler Seed

**Files:**
- Create: `src/app/m/uav/_lib/seedLokal.ts`
- Modify: `scripts/seed-lokal.ts` (Import + Aufruf wie bei `qr`)
- Test: `scripts/seed-lokal.test.ts` wird grün (Dreieck), `src/app/m/uav/_lib/seedLokal.test.ts`

**Interfaces:**
- `seedLokalUav(db): Promise<string[]>` — idempotent per Entität, additiv: 3 Tasks (`1-1`, `1-2`, `2-1`) mit Bildern `/m/uav/illustrations/1-1.webp` …, 2 Teilnehmer mit **festen** Codes `E2ETEST1` (aktiv) und `E2EGESP2` (inaktiv), 2 Executions für `1-1`. Die Codes stehen in `e2e/helpers/uav.ts` (Task 21).

- [ ] **Step 1–5:** Test (zweimal laufen lassen → gleiche Zeilenzahl) → rot → Implementierung → grün → Commit `uav: lokaler Seed mit festen Testcodes`

### Task 20: Illustrationen übernehmen

**Files:**
- Create: `public/m/uav/illustrations/*.webp` (Kopie aus `uav-praxis/public/illustrations/`)
- Test: `src/app/m/uav/illustrationen.test.ts`

- [ ] **Step 1: Test** — jede `tasks`-Zeile des Seeds hat eine Datei; und `decideRoute({host: "uav-training.iuk-ue.de", pathname: "/m/uav/illustrations/1-1.webp", groups: null})` ist `{action:"next"}` (interner Pfad, öffentliches Modul) — das ist der Beweis, dass `<img src="/m/uav/illustrations/…">` auf dem uav-Host trifft.
- [ ] **Step 2–5:** `cp ../uav-praxis/public/illustrations/*.webp public/m/uav/illustrations/` → grün → Commit `uav: Illustrationen aus der Alt-Anwendung übernommen`

### Task 21: Playwright

**Files:**
- Create: `e2e/helpers/uav.ts`, `e2e/uav.spec.ts`
- Modify: `playwright.config.ts` webServer-`env`: `SUITE_ADMIN_GROUP_UAV: "uav-training-admin"`, `UAV_SW_MODUS: "cachen"`

**Interfaces:**
- Helpers: `UAV_HOST = "uav.localtest.me"`, `uavUrl(pfad)`, `UAV_ADMIN_GRUPPE`, `E2E_CODE_AKTIV = "E2ETEST1"`, `E2E_CODE_INAKTIV = "E2EGESP2"`.

- [ ] **Step 1: Specs**

```ts
test("Magic-Link löst den Code ein und landet auf dem Dashboard", async ({ page }) => {
  await page.goto(uavUrl(`/login?code=${E2E_CODE_AKTIV.toLowerCase()}`));
  await expect(page.getByRole("heading", { name: /Training/ })).toBeVisible();
  const sid = (await page.context().cookies(uavUrl("/"))).find((c) => c.name === "sid");
  expect(sid?.path).toBe("/"); expect(sid?.httpOnly).toBe(true); expect(sid?.domain).toBe("uav.localtest.me");
});
test("/login ohne code auf dem uav-Host zeigt den Suite-Login", async ({ page }) => {
  await page.goto(uavUrl("/login"));
  await expect(page.getByRole("button", { name: "Dev-Login" })).toBeVisible();
});
test("inaktiver Code wird abgewiesen", …);
test("Erfassung offline → online → in der Verwaltung sichtbar", async ({ page, context }) => {
  // Login, Aufgabe öffnen, context.setOffline(true), Durchführung erfassen (bleibt in der Queue),
  // setOffline(false), auf Status 'synced' warten, dann als Admin (devLogin mit Gruppe) das Detail öffnen: anzahl 1.
});
test("Verwaltung: ohne Gruppe 404, mit Gruppe Liste; fremder Host 404 für /m/uav/api/admin/participants", …);
test("/sw.js liefert auf dem uav-Host JavaScript, auf einem fremden 404", …);
```

- [ ] **Step 2–5:** rot → Anpassungen → `pnpm e2e --grep uav` grün → Commit `uav: Playwright — Magic-Link, Offline-Sync, Gating, Host-Riegel`

### Task 22: Runbook, Progress-Doc, Release-Notiz

**Files:**
- Create: `docs/runbooks/uav-cutover.md`
- Modify: `/Users/rubeen/dev/personal/drk/KONSOLIDIERUNG-PROGRESS.md` (neuer Abschnitt „Phase 8 — Modul `uav`", Cutover-Tabelle: Zeile `uav-training.iuk-ue.de | uav | uav-praxis (alt) | ⬜`)
- Modify: Release-Notizen des Portals (Muster `ee2afa52`: „radio meldet seinen Einzug")

**Inhalt des Runbooks** (Muster `qr-cutover.md`, Abschnitte A–H):
- **§A Vorab (Betreiber):** Gruppe `uav-training-admin` in Pocket ID + Mitglieder · `.env`: `SUITE_HOST_UAV=uav-training.iuk-ue.de`, `SUITE_ADMIN_GROUP_UAV=uav-training-admin`, `UAV_SW_MODUS=abraeumen`; Host in `SUITE_TRAEFIK_RULE` **noch nicht** (erst §C) · Snapshot: `docker exec <uav-praxis> sqlite3 /app/data/app.db ".backup /app/data/snap.db"` (WAL!) · Zählungen: `select count(*) from participants; … executions; … task_status; … sessions where kind='participant' and expires_at > datetime('now');` — die Zahlen hier eintragen.
- **§B Deploy 1:** Merge → Rollout → `curl -sI https://iuk-ue.de/api/health/uav` (revision = Merge-SHA). Der uav-Host zeigt noch die Alt-App.
- **§P Generalprobe:** lokal `DATA_DIR=./.data/gp pnpm import:uav snap.db` → Paritätsbericht grün → `pnpm dev` mit `SUITE_HOST_UAV=uav.localtest.me` → einen Magic-Link aus `snap.db` (`select login_code from participants where aktiv=1 limit 1`) auf `http://uav.localtest.me:3000/login?code=…` öffnen → Dashboard.
- **§C Cutover:** `docker stop <uav-praxis>` (= Freeze, DB konsistent) → Snapshot **erneut** → Import auf dem Server (`docker compose exec suite pnpm import:uav /data/snap.db` — Pfad nach Server-Layout) → Parität grün → Traefik-Regel + `up -d` → **Verify:** `curl -sI -H "Host: uav-training.iuk-ue.de" …/` = 200, `/api/health/uav`, `/sw.js` enthält `unregister`, **ein echter Magic-Link vom Handy**, ein Handy mit alter Installation neu laden → Alt-Oberfläche verschwindet, Fortschritt bleibt.
- **§S Standby 14 Tage:** Rollback = Traefik-Regel zurück + `docker start <uav-praxis>`; ⚠️ nach Rollback sind Sync-Schreibungen aus der Suite-Zeit in der Alt-DB nicht enthalten.
- **§E Abbau:** `UAV_SW_MODUS=cachen` + `up -d` → Container/Volume/Repo archivieren → alten OIDC-Client in Pocket ID löschen → `.env` der Alt-App löschen (**FAL_KEY** darin, Spec §6).
- **§H Offene Betreiberzeilen** (Tabelle aus Spec §8).

- [ ] **Step 1–3:** Runbook schreiben → Progress-Doc ergänzen → Release-Notiz → Commit `uav: Cutover-Runbook, Fortschrittsdokument, Release-Notiz`

---

## Selbstprüfung des Plans gegen die Spec

| Spec | Task |
|---|---|
| §1 Registry, Gruppe, Shells, Illustrationen | 1, 12, 15, 20 |
| §2 Schema 1:1, kein Seed-Katalog, Import inkl. Sessions, Parität mit Idempotenz-Änderung | 2, 18, 19 |
| §3 #1 Code | 3 · #2 Brücke | 7, 13 · #3 Cookie/Hash | 4, 8 · #4 Storage-Keys/Queue/Abräum-Worker | 11, 14 |
| §4 API inkl. Rate-Limit pro Code, participantId aus Session, CSV-Neutralisierung | 8, 9, 10 |
| §5 Teilnehmer (kein antd), Offline-Worker, Modus-Umschalter, Admin (antd), Magic-Link aus Prod-Host | 12, 13, 14, 15, 16, 17 |
| §6 Runbook, Health, Backup (`*.db`, keine Änderung) | 22 (Health: `/api/health/[modul]` ist generisch — kein Task nötig) |
| §7 Tests inkl. Mutation, Playwright | in jedem Task; 21 |
| §8 Betreiberzeilen | 22 |

Bewusst **nicht** im Plan: Admin-Sessions der Alt-App (verfallen, Spec §2) · `ADMIN_ALLOWLIST` · `since` im Sync (wird angenommen, nie ausgewertet — wie in der Alt-App) · TanStack-Loader-Struktur.
